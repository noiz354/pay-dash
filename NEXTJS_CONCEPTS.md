# Next.js Concepts — 210 Entries

| # | Concept | TL;DR | Use Case | TS Snippet |
|---|---|---|---|---|
| 1 | App Router | The App Router is Next.js's modern file-system router built around layouts, React Server Components, Suspense, and Server Functions. | Build a dashboard with nested layouts and server-rendered pages. | `export default function Page(){ return <main>Dashboard</main>; }` |
| 2 | Pages Router | The Pages Router maps files inside `pages` to routes and supports legacy data-fetching APIs. | Maintain an established application using `getServerSideProps`. | `export const getServerSideProps:GetServerSideProps=async()=>({props:{}});` |
| 3 | Server Components | Server Components render on the server, access backend resources directly, and send no component JavaScript to the browser. | Query a database without exposing credentials or ORM code. | `export default async function Page(){ return <Posts posts={await db.post.findMany()}/>; }` |
| 4 | Client Components | Client Components opt into browser-side JavaScript for state, effects, events, and browser APIs. | Implement an interactive quantity selector. | `'use client'; export function Counter(){ const [n,setN]=useState(0); return <button onClick={()=>setN(n+1)}>{n}</button>; }` |
| 5 | Server Actions | Server Actions are server-only functions invoked through forms or transitions to perform mutations. | Create a database record from a product form. | `'use server'; export async function createProduct(data:FormData){ await db.product.create({data:{name:String(data.get('name'))}}); }` |
| 6 | Route Handlers | Route Handlers expose HTTP endpoints through `route.ts` files using Web Request and Response APIs. | Provide a JSON endpoint for mobile clients. | `export async function GET(){ return Response.json({status:'ok'}); }` |
| 7 | Middleware | Middleware is the deprecated name for request-boundary logic renamed to Proxy in Next.js 16. | Redirect unauthenticated visitors before protected routes execute. | `export function proxy(req:NextRequest){ return req.cookies.has('session')?NextResponse.next():NextResponse.redirect(new URL('/login',req.url)); }` |
| 8 | Layouts | Layouts wrap route segments, preserve state across navigation, and compose nested user interfaces. | Keep a dashboard sidebar mounted between pages. | `export default function Layout({children}:{children:React.ReactNode}){ return <Shell>{children}</Shell>; }` |
| 9 | Templates | Templates resemble layouts but remount their children when the associated route segment changes. | Reset an onboarding form on each step transition. | `export default function Template({children}:{children:React.ReactNode}){ return <section>{children}</section>; }` |
| 10 | Nested Routing | Nested routing maps nested folders to nested URL segments and layout hierarchies. | Organize `/dashboard/settings/profile` under a shared settings layout. | `const route='app/dashboard/settings/profile/page.tsx' as const;` |
| 11 | Dynamic Routes | Dynamic routes use bracketed segment names to capture variable URL parameters. | Render a product page from `/products/sku-123`. | `export default async function Page({params}:PageProps<'/products/[id]'>){ return <p>{(await params).id}</p>; }` |
| 12 | Catch-all Routes | A catch-all segment captures one or more remaining URL segments as an array. | Render arbitrarily nested documentation pages. | `export default async function Page({params}:PageProps<'/docs/[...slug]'>){ return <p>{(await params).slug.join('/')}</p>; }` |
| 13 | Optional Catch-all Routes | An optional catch-all segment matches both its base route and any nested segments. | Handle `/shop` and `/shop/electronics/phones` with one page. | `export default async function Page({params}:PageProps<'/shop/[[...slug]]'>){ return <p>{(await params).slug?.join('/')??'all'}</p>; }` |
| 14 | Parallel Routes | Parallel Routes render multiple named slots simultaneously within one layout. | Display dashboard analytics and activity panels independently. | `export default function Layout({children,analytics}:{children:React.ReactNode;analytics:React.ReactNode}){ return <>{children}{analytics}</>; }` |
| 15 | Intercepting Routes | Intercepting Routes display another route inside the current layout during soft navigation. | Open a photo in a modal while preserving the feed behind it. | `const interceptedRoute='app/feed/@modal/(..)photo/[id]/page.tsx' as const;` |
| 16 | Static Site Generation | SSG prerenders route output ahead of requests for fast cacheable delivery. | Publish a marketing page whose content changes only during deployments. | `export default function Page(){ return <h1>About Acme</h1>; }` |
| 17 | Server-Side Rendering | SSR generates route output at request time using request-specific or frequently changing data. | Render a personalized account page from cookies. | `export default async function Page(){ const session=(await cookies()).get('session'); return <Account session={session?.value}/>; }` |
| 18 | Incremental Static Regeneration | ISR refreshes previously generated content after a configured interval without rebuilding the entire application. | Refresh CMS articles hourly while serving cached pages. | `const posts=await fetch(API,{next:{revalidate:3600}}).then(r=>r.json() as Promise<Post[]>);` |
| 19 | Client-Side Rendering | CSR fetches or computes content in the browser after the initial page loads. | Load user-specific notifications after hydration. | `'use client'; const {data}=useSWR<Notification[]>('/api/notifications',fetcher);` |
| 20 | Partial Prerendering | PPR sends a static shell immediately while Suspense-delimited dynamic regions stream later through Cache Components. | Serve a static product shell with a dynamic shopping cart. | `const nextConfig:NextConfig={cacheComponents:true};` |
| 21 | Streaming | Streaming progressively sends server-rendered UI chunks as their data becomes available. | Show product details before slower recommendations finish loading. | `export default function Page(){ return <Suspense fallback={<Skeleton/>}><Recommendations/></Suspense>; }` |
| 22 | Suspense Boundaries | Suspense boundaries define loading fallbacks around components that suspend during rendering. | Isolate a slow analytics widget from the rest of a dashboard. | `<Suspense fallback={<ChartSkeleton/>}><RevenueChart/></Suspense>` |
| 23 | Edge Runtime | The Edge Runtime executes supported server code in lightweight geographically distributed isolates with restricted Node.js APIs. | Perform a low-latency geolocation rewrite near the visitor. | `export const runtime='edge';` |
| 24 | Node.js Runtime | The Node.js runtime supports standard Node APIs and is the default server runtime for most Next.js workloads. | Use Prisma, filesystem access, or Node-specific SDKs. | `export const runtime='nodejs';` |
| 25 | Static Exports | Static export emits deployable HTML, CSS, JavaScript, and assets without requiring a Next.js server. | Host a documentation site on static object storage. | `const nextConfig:NextConfig={output:'export'};` |
| 26 | `page.tsx` | `page.tsx` defines the publicly accessible UI for its route segment. | Create the `/pricing` page. | `export default function Page(){ return <Pricing/>; }` |
| 27 | `layout.tsx` | `layout.tsx` defines persistent shared UI around pages and nested layouts. | Share navigation across all dashboard routes. | `export default function Layout({children}:{children:React.ReactNode}){ return <DashboardNav>{children}</DashboardNav>; }` |
| 28 | `loading.tsx` | `loading.tsx` provides an automatic Suspense fallback for its route segment. | Display skeleton cards while a catalog route streams. | `export default function Loading(){ return <CatalogSkeleton/>; }` |
| 29 | `error.tsx` | `error.tsx` is a Client Component error boundary for failures within its route segment. | Show a retry button when a dashboard query fails. | `'use client'; export default function Error({reset}:{reset:()=>void}){ return <button onClick={reset}>Retry</button>; }` |
| 30 | `not-found.tsx` | `not-found.tsx` renders segment-specific UI when `notFound()` is invoked or a route is unmatched. | Display a branded missing-product page. | `export default function NotFound(){ return <h1>Product not found</h1>; }` |
| 31 | `route.ts` | `route.ts` defines HTTP handlers for a route segment without rendering UI. | Receive a payment-provider webhook. | `export async function POST(req:Request){ const event=await req.json(); return Response.json({received:Boolean(event)}); }` |
| 32 | `middleware.ts` | `middleware.ts` is the legacy request interception convention replaced by `proxy.ts` in Next.js 16. | Preserve request interception while migrating an older project. | `export {proxy as middleware} from './proxy';` |
| 33 | `template.tsx` | `template.tsx` wraps a segment like a layout but receives a new identity during matching navigation changes. | Restart entrance animations whenever a route changes. | `export default function Template({children}:{children:React.ReactNode}){ return <AnimatedPage>{children}</AnimatedPage>; }` |
| 34 | `default.tsx` | `default.tsx` supplies fallback UI for a Parallel Route slot when its active state cannot be recovered. | Prevent a modal slot from causing a hard-refresh build error. | `export default function Default(){ return null; }` |
| 35 | `global-error.tsx` | `global-error.tsx` is a root-level Client Component boundary that replaces the root layout after an uncaught error. | Recover from failures affecting the entire application shell. | `'use client'; export default function GlobalError(){ return <html><body><h1>Unexpected error</h1></body></html>; }` |
| 36 | `instrumentation.ts` | `instrumentation.ts` initializes observability or monitoring code when a server instance starts. | Register OpenTelemetry tracing before handling requests. | `export async function register(){ await import('./instrumentation.node'); }` |
| 37 | `sitemap.ts` | `sitemap.ts` programmatically generates standards-compatible sitemap entries. | Generate search-engine URLs from database products. | `export default function sitemap():MetadataRoute.Sitemap{ return [{url:'https://example.com',lastModified:new Date()}]; }` |
| 38 | `robots.ts` | `robots.ts` generates crawler directives and optional sitemap locations. | Block search engines from indexing an admin area. | `export default function robots():MetadataRoute.Robots{ return {rules:{userAgent:'*',disallow:'/admin/'}}; }` |
| 39 | `manifest.ts` | `manifest.ts` generates a typed web app manifest for installation metadata. | Make a storefront installable as a PWA. | `export default function manifest():MetadataRoute.Manifest{ return {name:'Store',short_name:'Store',start_url:'/',display:'standalone'}; }` |
| 40 | `opengraph-image.tsx` | `opengraph-image.tsx` statically or dynamically generates social-sharing images for a route. | Create article-specific preview cards. | `export default function Image(){ return new ImageResponse(<div>Acme Blog</div>); }` |
| 41 | `<Image>` | `next/image` optimizes image sizing, formats, loading behavior, and layout stability. | Deliver responsive product photographs without layout shift. | `<Image src="/shoe.jpg" alt="Running shoe" width={800} height={600}/>` |
| 42 | `<Link>` | `next/link` enables accessible client-side navigation with automatic prefetching behavior. | Navigate from a catalog card to its product page. | `<Link href="/products/42">View product</Link>` |
| 43 | `<Script>` | `next/script` controls when and how third-party JavaScript loads. | Load analytics after the page becomes interactive. | `<Script src="https://cdn.example.com/analytics.js" strategy="afterInteractive"/>` |
| 44 | `next/font` | `next/font` self-hosts and optimizes local or Google fonts while reducing layout shift. | Apply Inter throughout an application without external font requests. | `const inter=Inter({subsets:['latin'],display:'swap'});` |
| 45 | `<Head>` | `next/head` manages document head tags in the legacy Pages Router. | Add a page title to an existing Pages Router application. | `<Head><title>Account settings</title></Head>` |
| 46 | Metadata API | The Metadata API defines typed SEO and document metadata from App Router layouts or pages. | Configure default title templates and descriptions. | `export const metadata:Metadata={title:'Acme',description:'Acme storefront'};` |
| 47 | Dynamic Metadata | Dynamic metadata computes SEO information from route parameters or fetched data. | Generate a product-specific title and Open Graph description. | `export async function generateMetadata({params}:PageProps<'/products/[id]'>):Promise<Metadata>{ return {title:(await params).id}; }` |
| 48 | `generateStaticParams` | `generateStaticParams` declares dynamic parameter combinations that Next.js can prerender. | Prebuild pages for every published article slug. | `export async function generateStaticParams(){ return db.post.findMany({select:{slug:true}}); }` |
| 49 | `generateMetadata` | `generateMetadata` asynchronously returns route metadata using params, search params, or fetched data. | Read a CMS article title for browser and social metadata. | `export async function generateMetadata():Promise<Metadata>{ return {title:(await getPost()).title}; }` |
| 50 | `revalidatePath` / `revalidateTag` | These server APIs invalidate cached output by route path or cache tag after data changes. | Refresh a product page and shared catalog after an admin update. | `revalidatePath('/products/42'); revalidateTag('products','max');` |
| 51 | `fetch()` with caching | Next.js can opt server-side `fetch` requests into persistent caching, timed revalidation, and cache tags. | Cache a headless CMS catalog for one hour. | `const products=await fetch(API,{cache:'force-cache',next:{revalidate:3600,tags:['products']}}).then(r=>r.json() as Promise<Product[]>);` |
| 52 | `cache()` from React | React `cache` memoizes a server function’s result for matching arguments during a render request. | Prevent multiple components from repeating the same user query. | `export const getUser=cache((id:string)=>db.user.findUnique({where:{id}}));` |
| 53 | `unstable_cache` | `unstable_cache` persistently caches arbitrary async operations but is superseded by `use cache` in Next.js 16. | Maintain caching in an application not yet migrated to Cache Components. | `const getProducts=unstable_cache(()=>db.product.findMany(),['products'],{tags:['products']});` |
| 54 | `use()` Hook | React's `use` reads a promise or context during render and suspends while an unresolved promise completes. | Unwrap a server-created promise inside a Client Component. | `const products=use(productsPromise);` |
| 55 | Data Cache | The Data Cache persists cached server data across requests and can be revalidated independently of rendered routes. | Share cached CMS data among several pages. | `async function getCatalog(){ 'use cache'; return db.product.findMany(); }` |
| 56 | Full Route Cache | The Full Route Cache stores prerendered HTML and React Server Component payloads for reusable routes. | Serve an unchanged company page without rendering it per request. | `'use cache'; export default async function Page(){ return <About data={await getAbout()}/>; }` |
| 57 | Router Cache | The Router Cache stores prefetched and visited route-segment payloads in browser memory for faster navigation. | Make back navigation between catalog pages feel immediate. | `const router=useRouter(); router.prefetch('/dashboard');` |
| 58 | Request Memoization | Request memoization deduplicates matching server `fetch` calls during one React rendering pass. | Let metadata and page content request the same article safely. | `const getPost=()=>fetch(API+'/post/42').then(r=>r.json() as Promise<Post>);` |
| 59 | `no-store` Opt-out | `cache: 'no-store'` forces a fetch to retrieve fresh data instead of using the persistent data cache. | Display a current account balance. | `const balance=await fetch(API+'/balance',{cache:'no-store'}).then(r=>r.json() as Promise<Balance>);` |
| 60 | Tag-based Revalidation | Cache tags group related cached data for targeted on-demand invalidation using stale-while-revalidate or immediate update semantics. | Refresh all product views after inventory synchronization. | `revalidateTag('products','max');` |
| 61 | `useRouter` | `useRouter` provides imperative App Router navigation, refresh, prefetch, back, and forward operations. | Redirect to an order confirmation after checkout. | `const router=useRouter(); router.push('/orders/complete');` |
| 62 | `usePathname` | `usePathname` returns the current URL pathname inside a Client Component. | Highlight the active navigation item. | `const pathname=usePathname(); const active=pathname==='/settings';` |
| 63 | `useSearchParams` | `useSearchParams` exposes a read-only interface to the current query string in Client Components. | Read a catalog sorting option from `?sort=price`. | `const sort=useSearchParams().get('sort')??'featured';` |
| 64 | `useParams` | `useParams` returns the active dynamic route parameters in a Client Component. | Read a product identifier in an interactive product widget. | `const {id}=useParams<{id:string}>();` |
| 65 | `useSelectedLayoutSegment` | This hook returns the active child segment one level below the layout where it is used. | Mark the current dashboard tab as selected. | `const segment=useSelectedLayoutSegment();` |
| 66 | `useSelectedLayoutSegments` | This hook returns all active child segments below the layout where it is called. | Build breadcrumbs from nested route state. | `const segments=useSelectedLayoutSegments();` |
| 67 | `useFormStatus` | `useFormStatus` exposes the submission state of the nearest parent form. | Disable a checkout button while its Server Action executes. | `const {pending}=useFormStatus();` |
| 68 | `useFormState` / `useActionState` | `useActionState` manages a Server Action's returned state, with `useFormState` being its older predecessor. | Display validation errors returned by a registration action. | `const [state,formAction,pending]=useActionState(register,initialState);` |
| 69 | `useOptimistic` | `useOptimistic` displays an anticipated state immediately while an asynchronous mutation completes. | Show a newly posted comment before the server responds. | `const [comments,addOptimistic]=useOptimistic(initialComments,(state,item:Comment)=>[...state,item]);` |
| 70 | `useTransition` | `useTransition` schedules non-urgent state or navigation work while exposing its pending status. | Keep controls responsive while refreshing filtered results. | `const [pending,startTransition]=useTransition();` |
| 71 | Vercel | Vercel is the platform created by the Next.js team with native deployment, caching, preview, and observability integrations. | Deploy each pull request to an isolated preview environment. | `export const preferredRegion=['iad1'] as const;` |
| 72 | Netlify | Netlify deploys Next.js applications using framework-aware builds and serverless or edge infrastructure. | Host a marketing application with preview deployments. | `const netlifyBuild={command:'npm run build',publish:'.next'} as const;` |
| 73 | AWS Amplify | AWS Amplify Hosting builds and deploys supported Next.js applications into AWS-managed infrastructure. | Host a Next.js frontend alongside Cognito and AppSync. | `const amplifyBuildCommand='npm run build' as const;` |
| 74 | Cloudflare | Cloudflare deploys Next.js through Workers-compatible adapters such as OpenNext for Cloudflare. | Run a globally distributed storefront near customers. | `const cloudflareBuild='opennextjs-cloudflare build' as const;` |
| 75 | Railway | Railway runs self-hosted Next.js Node servers with managed builds, networking, and environment variables. | Deploy an internal dashboard connected to Railway PostgreSQL. | `const railwayStart='next start' as const;` |
| 76 | Render | Render hosts Next.js as a Node web service or supported static site. | Deploy a server-rendered customer portal from GitHub. | `const renderCommand='npm run start' as const;` |
| 77 | DigitalOcean App Platform | DigitalOcean App Platform builds and operates containerized or Node-based Next.js services. | Host a SaaS frontend near DigitalOcean-managed databases. | `const runCommand='npm run start' as const;` |
| 78 | Self-hosted Docker/Node | Self-hosting runs the generated Next.js server on infrastructure controlled by your organization. | Deploy a standalone build to Kubernetes. | `const nextConfig:NextConfig={output:'standalone'};` |
| 79 | OpenNext | OpenNext adapts Next.js build artifacts and runtime behavior for non-Vercel cloud infrastructure. | Deploy App Router functions and caches to AWS. | `const adapterPackage='@opennextjs/aws' as const;` |
| 80 | SST | SST provisions full-stack applications and can deploy Next.js resources to AWS using infrastructure as code. | Deploy a Next.js SaaS with AWS secrets and databases. | `const web=new sst.aws.Nextjs('Web');` |
| 81 | shadcn/ui | shadcn/ui provides accessible component source code generated into and owned by your project. | Build a customizable admin interface without a runtime UI dependency. | `import {Button} from '@/components/ui/button';` |
| 82 | Tailwind CSS | Tailwind CSS is a utility-first styling engine commonly integrated with Next.js. | Create responsive product cards without separate component stylesheets. | `const cardClass='grid gap-4 rounded-xl p-6 md:grid-cols-2';` |
| 83 | Material UI | Material UI provides production-ready React components implementing Material Design. | Build a dense enterprise administration portal. | `const button=<Button variant="contained">Save</Button>;` |
| 84 | Chakra UI | Chakra UI offers accessible React components with prop-based styling and theming. | Build a themeable account settings interface. | `const alert=<Alert status="error">Save failed</Alert>;` |
| 85 | Ant Design | Ant Design is an enterprise-focused React component system with extensive data-entry widgets. | Build an inventory management table. | `const table=<Table<Product> rowKey="id" dataSource={products} columns={columns}/>;` |
| 86 | Mantine | Mantine provides typed React components, hooks, forms, notifications, and theming. | Implement a feature-rich internal operations dashboard. | `const input=<TextInput label="Email" type="email" required/>;` |
| 87 | HeroUI | HeroUI is a Tailwind-based React component library formerly known as NextUI. | Build a polished customer-facing SaaS interface. | `const button=<Button color="primary">Upgrade</Button>;` |
| 88 | Radix UI | Radix UI supplies unstyled accessible primitives for custom design systems. | Build an accessible modal with complete visual control. | `const trigger=<Dialog.Trigger>Edit profile</Dialog.Trigger>;` |
| 89 | Headless UI | Headless UI provides unstyled accessible interactive components designed for custom styling. | Create a keyboard-accessible account menu. | `const menu=<Menu><MenuButton>Account</MenuButton></Menu>;` |
| 90 | Aceternity UI | Aceternity UI is a collection of copyable animated interface components commonly paired with Tailwind and Motion. | Add animated hero effects to a landing page. | `import {BackgroundBeams} from '@/components/ui/background-beams';` |
| 91 | Magic UI | Magic UI provides copyable animated components for modern React and Next.js landing pages. | Add an animated customer-logo marquee. | `import {Marquee} from '@/components/magicui/marquee';` |
| 92 | Tremor | Tremor provides React components optimized for dashboards, charts, and business metrics. | Display monthly recurring revenue trends. | `const chart=<AreaChart data={revenue} index="month" categories={['mrr']}/>;` |
| 93 | Park UI | Park UI offers generated, accessible components built with Ark UI and Panda CSS. | Establish a type-safe internal design system. | `import {Button} from '@/components/ui/button';` |
| 94 | DaisyUI | DaisyUI adds semantic component classes and themes on top of Tailwind CSS. | Prototype a styled admin panel quickly. | `const button=<button className="btn btn-primary">Save</button>;` |
| 95 | Flowbite | Flowbite provides Tailwind-based components and a React package for common application interfaces. | Build a responsive navigation bar and modal. | `const modal=<Modal show={open} onClose={()=>setOpen(false)}/>;` |
| 96 | Auth.js | Auth.js provides configurable authentication, sessions, OAuth providers, and route handlers for Next.js. | Add Google and GitHub sign-in to a SaaS product. | `export const {handlers,auth,signIn,signOut}=NextAuth({providers:[GitHub]});` |
| 97 | Clerk | Clerk supplies hosted authentication, user management, organizations, and prebuilt Next.js components. | Add multi-tenant organization accounts. | `const {userId,orgId}=await auth();` |
| 98 | Supabase Auth | Supabase Auth provides password, OAuth, magic-link, and token-based authentication backed by Supabase. | Authenticate customers who also use Supabase data. | `const {data:{user}}=await supabase.auth.getUser();` |
| 99 | Auth0 | Auth0 is a managed identity platform with Next.js SDK support for sessions and enterprise providers. | Add enterprise SSO to a customer portal. | `const auth0=new Auth0Client();` |
| 100 | Kinde | Kinde provides managed authentication, organizations, permissions, and billing-oriented identity features. | Add authentication to an early-stage B2B SaaS. | `const {getUser}=getKindeServerSession();` |
| 101 | Lucia Auth | Lucia is a legacy authentication library whose patterns remain useful for understanding session implementation. | Maintain an older application already using Lucia sessions. | `const {session,user}=await lucia.validateSession(sessionId);` |
| 102 | Firebase Auth | Firebase Auth supplies managed identity providers and client or admin authentication SDKs. | Add phone and Google authentication to a consumer application. | `const auth=getAuth(firebaseApp);` |
| 103 | Better Auth | Better Auth is a TypeScript-first authentication framework supporting sessions, social providers, and plugins. | Implement self-hosted authentication with database-backed sessions. | `export const auth=betterAuth({database:db});` |
| 104 | Stack Auth | Stack Auth provides open-source authentication components and managed or self-hosted identity infrastructure. | Add teams and account management to a startup dashboard. | `export const stackServerApp=new StackServerApp({tokenStore:'nextjs-cookie'});` |
| 105 | WorkOS | WorkOS provides enterprise SSO, directory synchronization, audit logs, and AuthKit authentication. | Let corporate customers sign in through SAML. | `const {user}=await withAuth();` |
| 106 | Prisma | Prisma is a type-safe ORM with schema-driven clients, migrations, and database tooling. | Query PostgreSQL products from a Server Component. | `const products=await prisma.product.findMany({where:{active:true}});` |
| 107 | Drizzle ORM | Drizzle is a lightweight TypeScript ORM using SQL-like typed schemas and queries. | Query a serverless PostgreSQL database with minimal runtime overhead. | `const products=await db.select().from(productsTable);` |
| 108 | Supabase | Supabase combines managed PostgreSQL, APIs, storage, realtime events, and authentication. | Build a collaborative application with realtime database updates. | `const {data,error}=await supabase.from('tasks').select('*').throwOnError();` |
| 109 | PlanetScale | PlanetScale is a managed database platform designed for scalable production database workflows. | Operate a high-traffic commerce database with branching workflows. | `const conn=mysql.createConnection({url:process.env.DATABASE_URL});` |
| 110 | Neon | Neon provides serverless PostgreSQL with branching, autoscaling, and scale-to-zero capabilities. | Give every preview deployment an isolated database branch. | `const sql=neon(process.env.DATABASE_URL!);` |
| 111 | MongoDB with Mongoose | Mongoose provides schemas, validation, models, and query APIs over MongoDB. | Store flexible product catalog documents. | `const product=await ProductModel.findById(id).lean<Product>();` |
| 112 | Turso | Turso provides distributed SQLite databases through the libSQL protocol. | Place read-heavy application data near global users. | `const client=createClient({url:process.env.TURSO_URL!,authToken:process.env.TURSO_TOKEN});` |
| 113 | Vercel Postgres | Vercel Postgres refers to Vercel-integrated managed PostgreSQL access, now generally fulfilled through marketplace providers. | Connect a Vercel deployment to managed PostgreSQL. | `const rows=await sql<Product>\`SELECT * FROM products WHERE active = true\`;` |
| 114 | Kysely | Kysely is a type-safe SQL query builder that preserves direct control over generated SQL. | Build complex reporting queries without a full ORM. | `const users=await db.selectFrom('user').selectAll().execute();` |
| 115 | TypeORM | TypeORM is a decorator and Data Mapper-style ORM supporting several SQL databases. | Maintain an entity-based enterprise application. | `const users=await dataSource.getRepository(User).find();` |
| 116 | Zustand | Zustand is a small hook-based state manager using independently defined stores. | Share shopping-cart state among Client Components. | `const useCart=create<CartState>(set=>({items:[],add:item=>set(s=>({items:[...s.items,item]}))}));` |
| 117 | Redux Toolkit | Redux Toolkit is the official batteries-included approach for predictable Redux state management. | Manage complex normalized workflow state with debugging support. | `const store=configureStore({reducer:{cart:cartReducer}});` |
| 118 | Jotai | Jotai models state as composable atoms consumed through React hooks. | Manage small independent pieces of editor state. | `const selectedIdAtom=atom<string|null>(null);` |
| 119 | Recoil | Recoil is an atom-and-selector state library originally created for React applications. | Maintain an existing application built around Recoil atoms. | `const userAtom=atom<User|null>({key:'user',default:null});` |
| 120 | Valtio | Valtio provides proxy-based mutable state with reactive React snapshots. | Manage interactive canvas controls with minimal boilerplate. | `const state=proxy({zoom:1,selectedId:null as string|null});` |
| 121 | TanStack Query | TanStack Query manages asynchronous server state, caching, mutations, and background synchronization. | Cache paginated customer records in an interactive table. | `const query=useQuery({queryKey:['customers'],queryFn:getCustomers});` |
| 122 | SWR | SWR is Vercel's lightweight React hook for remote data caching and revalidation. | Poll a user's notification count from the browser. | `const {data}=useSWR<Notification[]>('/api/notifications',fetcher,{refreshInterval:30000});` |
| 123 | XState | XState models complex application behavior using actors and finite state machines. | Implement a checkout flow with explicit states and transitions. | `const checkoutMachine=createMachine({initial:'cart',states:{cart:{on:{NEXT:'payment'}},payment:{}}});` |
| 124 | MobX | MobX derives reactive application state through observable values and actions. | Maintain a legacy domain-model-heavy dashboard. | `const store=makeAutoObservable({count:0,increment(){this.count++;}});` |
| 125 | Nanostores | Nanostores provides framework-agnostic atomic stores with a very small client footprint. | Share cart state across Next.js and embedded widgets. | `const cart=$state<Cart>({items:[]});` |
| 126 | Jest | Jest is a JavaScript and TypeScript test runner with mocking, assertions, and snapshot support. | Unit-test a pricing utility used by Server Actions. | `test('totals items',()=>expect(total([{price:5}])).toBe(5));` |
| 127 | Vitest | Vitest is a fast Vite-powered test runner with Jest-compatible APIs. | Unit-test framework-independent application services. | `it('formats USD',()=>expect(formatUSD(5)).toBe('$5.00'));` |
| 128 | Playwright | Playwright automates Chromium, Firefox, and WebKit for end-to-end browser testing. | Verify a customer can complete checkout. | `await expect(page.getByRole('heading',{name:'Order confirmed'})).toBeVisible();` |
| 129 | Cypress | Cypress provides browser-based component and end-to-end testing with an interactive runner. | Test a registration journey in CI. | `cy.get('button[type="submit"]').click();` |
| 130 | React Testing Library | React Testing Library tests components through user-visible behavior instead of implementation details. | Verify an error message appears after invalid input. | `expect(screen.getByRole('alert')).toHaveTextContent('Invalid email');` |
| 131 | Mock Service Worker | MSW intercepts network requests in browsers or Node tests using realistic request handlers. | Test a dashboard against deterministic API responses. | `const server=setupServer(http.get('/api/users',()=>HttpResponse.json([])));` |
| 132 | Storybook | Storybook develops and documents UI components in isolated states. | Review every button variant without running the full application. | `const meta={component:Button} satisfies Meta<typeof Button>;` |
| 133 | Chromatic | Chromatic publishes Storybook and performs automated visual regression testing. | Detect unintended UI changes in pull requests. | `const parameters={chromatic:{viewports:[375,1440]}} as const;` |
| 134 | Testing Library User Event | `user-event` simulates realistic user interactions for Testing Library tests. | Test keyboard input and form submission behavior. | `await userEvent.setup().type(screen.getByLabelText('Email'),'dev@example.com');` |
| 135 | Puppeteer | Puppeteer controls Chromium programmatically for browser automation and rendering tasks. | Generate authenticated PDF reports from a Next.js route. | `const page=await browser.newPage(); await page.goto(reportUrl,{waitUntil:'networkidle0'});` |
| 136 | React Hook Form | React Hook Form provides performant uncontrolled form state and validation integration. | Build a large checkout form with minimal rerenders. | `const {register,handleSubmit}=useForm<CheckoutInput>();` |
| 137 | Formik | Formik manages React form values, validation, submission, and touched state. | Maintain an existing multi-field account form. | `const formik=useFormik<Profile>({initialValues,validate,onSubmit});` |
| 138 | Zod | Zod defines TypeScript-first runtime schemas for parsing and validating unknown data. | Validate Server Action input before writing to a database. | `const input=ProductSchema.parse(Object.fromEntries(formData));` |
| 139 | Yup | Yup provides composable runtime object schemas commonly used with form libraries. | Validate an established Formik registration form. | `const schema=yup.object({email:yup.string().email().required()});` |
| 140 | Valibot | Valibot provides modular, type-safe schema validation with tree-shakable functions. | Validate client and server input while minimizing bundle size. | `const result=safeParse(object({email:pipe(string(),email())}),input);` |
| 141 | Conform | Conform builds progressively enhanced, schema-validated forms around native HTML and server submissions. | Implement an accessible form backed by a Server Action. | `const [form,fields]=useForm({lastResult,onValidate:({formData})=>parseWithZod(formData,{schema})});` |
| 142 | TanStack Form | TanStack Form provides headless, type-safe form state and validation primitives. | Build a complex dynamic enterprise form. | `const form=useForm({defaultValues:{email:''},onSubmit:saveUser});` |
| 143 | React Final Form | React Final Form is a subscription-based form state manager designed to limit rerenders. | Maintain a form-heavy legacy React application. | `const form=<Form<Profile> onSubmit={save} render={({handleSubmit})=><form onSubmit={handleSubmit}/>}/>;` |
| 144 | Server Action Forms | Native forms can invoke Server Actions directly with progressive enhancement and server-side validation. | Create a contact request without a separate API endpoint. | `<form action={createContact}><input name="email" type="email" required/><button>Submit</button></form>` |
| 145 | Superforms | Superforms is primarily a SvelteKit form library rather than a native Next.js solution. | Share schemas with a separate SvelteKit admin application in a monorepo. | `const form=await superValidate(data,zod(schema));` |
| 146 | Motion | Motion, formerly Framer Motion, provides declarative React animations, gestures, transitions, and layout effects. | Animate a modal entering and leaving the viewport. | `const modal=<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}/>;` |
| 147 | GSAP | GSAP is an imperative high-performance animation platform for timelines and complex motion. | Build a scroll-driven product storytelling page. | `gsap.to(element,{x:120,opacity:1,duration:0.6});` |
| 148 | React Spring | React Spring provides physics-based animation primitives for React. | Animate a card using natural spring movement. | `const styles=useSpring({opacity:open?1:0,y:open?0:20});` |
| 149 | Auto-Animate | Auto-Animate automatically animates DOM changes with minimal configuration. | Smoothly animate cart items being added or removed. | `const [parent]=useAutoAnimate<HTMLUListElement>();` |
| 150 | Lottie React | Lottie React renders vector animations exported as Lottie JSON. | Display a lightweight animated checkout success illustration. | `const success=<Lottie animationData={successAnimation} loop={false}/>;` |
| 151 | Turbopack | Turbopack is Next.js's Rust-based incremental bundler and the default for development and production builds in Next.js 16. | Speed up compilation in a large monorepo. | `const nextConfig:NextConfig={turbopack:{resolveAlias:{'@ui':'./src/ui'}}};` |
| 152 | Webpack | Webpack is Next.js's legacy alternative bundler and requires the `--webpack` flag in current Next.js versions. | Retain a custom loader not yet compatible with Turbopack. | `const buildCommand='next build --webpack' as const;` |
| 153 | SWC | SWC is the Rust-based compiler used by Next.js for JavaScript, TypeScript, JSX, and framework transforms. | Remove console statements from production output. | `const nextConfig:NextConfig={compiler:{removeConsole:process.env.NODE_ENV==='production'}};` |
| 154 | ESLint Config for Next.js | `eslint-config-next` and `@next/eslint-plugin-next` provide framework-specific correctness and performance rules. | Detect unsafe `<img>` usage and navigation mistakes during CI. | `const config=defineConfig([...nextVitals]);` |
| 155 | `next lint` | `next lint` was removed in Next.js 16 and should be replaced with direct ESLint or Biome commands. | Migrate an existing CI lint job to ESLint CLI. | `const lintCommand='eslint .' as const;` |
| 156 | `next build` | `next build` creates an optimized production build and validates build-time rendering. | Generate production artifacts during CI. | `const scripts={build:'next build'} as const;` |
| 157 | `next dev` | `next dev` starts the development server with Fast Refresh and development diagnostics. | Run the application locally while implementing features. | `const scripts={dev:'next dev'} as const;` |
| 158 | `next start` | `next start` runs a previously generated production server build. | Smoke-test a production build before deployment. | `const scripts={start:'next start'} as const;` |
| 159 | Bundle Analyzer | Bundle analyzers visualize module sizes and import chains to identify expensive dependencies. | Find a charting package inflating a dashboard's client bundle. | `const analyzeCommand='next experimental-analyze' as const;` |
| 160 | `create-next-app` | `create-next-app` scaffolds a configured Next.js project with options such as TypeScript and App Router. | Start a new production application with standard defaults. | `const scaffold='create-next-app@latest acme --ts --app --eslint' as const;` |
| 161 | `next-intl` | `next-intl` provides App Router translations, localized routing, formatting, and server or client APIs. | Localize an e-commerce application into English and Spanish. | `const t=await getTranslations('Checkout');` |
| 162 | `next-i18next` | `next-i18next` integrates i18next with Next.js, particularly Pages Router applications. | Maintain a Pages Router site using JSON translation namespaces. | `export const getStaticProps=async(locale)=>({props:{...(await serverSideTranslations(locale,['common']))}});` |
| 163 | `next-translate` | `next-translate` provides namespace-based translations and build-time integration for Next.js. | Localize a legacy content site with translation JSON files. | `const {t}=useTranslation('common');` |
| 164 | Built-in i18n Routing | The Pages Router supports `i18n` configuration while App Router projects commonly model locales with a dynamic `[lang]` segment. | Route visitors between `/en/products` and `/es/products`. | `const nextConfig:NextConfig={i18n:{locales:['en','es'],defaultLocale:'en'}};` |
| 165 | Lingui | Lingui provides message extraction, catalogs, macros, and runtime internationalization for JavaScript applications. | Let translators manage extracted UI messages. | `const title=t\`Shopping cart\`;` |
| 166 | Sanity | Sanity is a structured-content platform with real-time editing and a queryable content lake. | Power editorial landing pages from a customizable studio. | `const posts=await sanityClient.fetch<Post[]>('*[_type=="post"]');` |
| 167 | Contentful | Contentful is a managed headless CMS exposing content through REST and GraphQL APIs. | Let marketing teams edit product campaign pages. | `const entries=await contentfulClient.getEntries<PostFields>({content_type:'post'});` |
| 168 | Strapi | Strapi is a self-hostable headless CMS that generates REST and GraphQL content APIs. | Manage articles and authors on organization-owned infrastructure. | `const posts=await fetch(CMS+'/api/posts?populate=*').then(r=>r.json() as Promise<PostsResponse>);` |
| 169 | Payload CMS | Payload is a TypeScript-first headless CMS that can run alongside or integrate directly with Next.js. | Build an admin-managed publishing platform in one repository. | `const payload=await getPayload({config});` |
| 170 | Headless WordPress | Headless WordPress uses WordPress for editorial management while Next.js renders the frontend through APIs. | Modernize a publisher without replacing its editorial workflow. | `const posts=await fetch(WP_GRAPHQL,{method:'POST',body:JSON.stringify({query:POSTS_QUERY})});` |
| 171 | Prismic | Prismic is a hosted page-oriented headless CMS using reusable slices and preview tooling. | Let marketers assemble landing pages from approved content blocks. | `const page=await client.getByUID('page',uid);` |
| 172 | Storyblok | Storyblok is a visual headless CMS built around components, stories, and live editing. | Give editors visual previews of marketing pages. | `const {data}=await storyblokApi.get('cdn/stories/home',{version:'published'});` |
| 173 | Hygraph | Hygraph is a GraphQL-native headless CMS for structured and federated content. | Query localized product editorial content through GraphQL. | `const data=await request<CatalogQuery>(HYGRAPH_URL,CATALOG_QUERY);` |
| 174 | Ghost | Ghost is a publishing platform exposing content through its Content API. | Render a company publication using a custom Next.js frontend. | `const posts=await ghost.posts.browse({limit:20,include:'tags'});` |
| 175 | Directus | Directus provides APIs and an administration layer over SQL databases. | Give business users controlled access to existing relational content. | `const posts=await directus.request(readItems('posts',{limit:20}));` |
| 176 | Stripe | Stripe provides payment processing, subscriptions, checkout, invoicing, and webhook APIs. | Create a hosted subscription checkout session. | `const session=await stripe.checkout.sessions.create({mode:'subscription',line_items:[{price:priceId,quantity:1}],success_url});` |
| 177 | Shopify Storefront API | Shopify's Storefront API enables a Next.js commerce frontend as an alternative to using the Hydrogen framework. | Build a custom Next.js storefront over a Shopify catalog. | `const response=await fetch(SHOPIFY_URL,{method:'POST',headers,body:JSON.stringify({query:PRODUCTS_QUERY})});` |
| 178 | Medusa.js | Medusa is an open-source commerce backend exposing modules and APIs for custom storefronts. | Build a Next.js storefront with self-hosted product and order services. | `const products=await fetch(MEDUSA_URL+'/store/products',{headers:{'x-publishable-api-key':key}}).then(r=>r.json());` |
| 179 | Lemon Squeezy | Lemon Squeezy provides merchant-of-record payments, subscriptions, licensing, and tax handling. | Sell a global SaaS subscription without managing sales tax directly. | `lemonSqueezySetup({apiKey:process.env.LEMONSQUEEZY_API_KEY!});` |
| 180 | Paddle | Paddle is a merchant-of-record platform for software payments, subscriptions, and tax compliance. | Launch international SaaS billing with hosted checkout. | `const paddle=await initializePaddle({environment:'production',token:clientToken});` |
| 181 | Next.js Official Docs | The official documentation is the primary reference for supported Next.js concepts, APIs, and migration guidance. | Verify current routing or caching behavior before implementation. | `const learningResource='Next.js official documentation' as const;` |
| 182 | Next.js Learn | Next.js Learn is an official guided course teaching framework fundamentals through practical projects. | Onboard a React developer to App Router patterns. | `const course='Next.js Learn' as const;` |
| 183 | Vercel YouTube Channel | Vercel's video channel publishes framework demonstrations, conference sessions, and platform guidance. | Watch a visual walkthrough of a newly introduced feature. | `const channel='Vercel' as const;` |
| 184 | Lee Robinson's Tutorials | Lee Robinson's educational content covers practical Next.js, React, performance, and platform architecture. | Learn production patterns from concise framework demonstrations. | `const author='Lee Robinson' as const;` |
| 185 | Josh W. Comeau's Blog | Josh W. Comeau's articles teach React, CSS, accessibility, and interaction techniques applicable to Next.js. | Improve animation and frontend UX fundamentals. | `const author='Josh W. Comeau' as const;` |
| 186 | Theo and T3 | Theo's content and T3 ecosystem emphasize type-safe full-stack TypeScript development. | Evaluate a Next.js, tRPC, Prisma, and authentication stack. | `const stack=['Next.js','TypeScript','tRPC'] as const;` |
| 187 | Jack Herrington | Jack Herrington publishes practical React and Next.js engineering tutorials. | Study implementation-focused App Router examples. | `const author='Jack Herrington' as const;` |
| 188 | Next.js Conf | Next.js Conf presents framework announcements, technical sessions, and production case studies. | Review architectural patterns introduced with a major release. | `const conference='Next.js Conf' as const;` |
| 189 | Reactathon | Reactathon is a React ecosystem conference covering frameworks, server rendering, and application architecture. | Learn how React platform developments affect Next.js. | `const conference='Reactathon' as const;` |
| 190 | Frontend Masters | Frontend Masters provides structured expert-led courses covering React, Next.js, TypeScript, and web performance. | Train a development team through a guided advanced course. | `const platform='Frontend Masters' as const;` |
| 191 | T3 Stack | The T3 Stack combines Next.js with type-safe tools such as TypeScript, tRPC, Prisma, and authentication. | Bootstrap a full-stack TypeScript SaaS. | `const scaffold='create-t3-app@latest' as const;` |
| 192 | Next.js Commerce | Next.js Commerce is a commerce-oriented reference starter demonstrating storefront patterns and integrations. | Prototype a headless online store. | `const starter='vercel/commerce' as const;` |
| 193 | Taxonomy | Taxonomy is a historical shadcn-built Next.js application showcasing App Router and component patterns. | Study a realistic content and commerce codebase. | `const referenceApp='shadcn-ui/taxonomy' as const;` |
| 194 | Precedent | Precedent is a historical opinionated Next.js starter featuring common SaaS and web application integrations. | Review established patterns for analytics, storage, and styling. | `const starter='steven-tey/precedent' as const;` |
| 195 | Next.js Boilerplate | A Next.js boilerplate packages repeatable configuration, architecture, testing, and tooling decisions. | Standardize how an organization starts new services. | `const template='organization/nextjs-boilerplate' as const;` |
| 196 | Next.js SaaS Starter | A SaaS starter combines authentication, billing, persistence, dashboards, and deployment conventions. | Validate a subscription product without rebuilding infrastructure basics. | `const features=['auth','billing','database'] as const;` |
| 197 | Vercel Templates Gallery | Vercel's template gallery provides deployable starter applications for supported frameworks and services. | Clone a working AI, commerce, or SaaS reference project. | `const templateSource='Vercel Templates' as const;` |
| 198 | Blitz.js | Blitz.js is a full-stack React toolkit historically built around Next.js conventions and zero-API data access. | Maintain an application created with the Blitz stack. | `const framework='Blitz.js' as const;` |
| 199 | RedwoodJS | RedwoodJS is a full-stack framework alternative with its own routing, data, and deployment architecture. | Compare opinionated full-stack frameworks before choosing Next.js. | `const alternative='RedwoodJS' as const;` |
| 200 | Payload CMS Starter | A Payload starter combines Payload's admin and content APIs with a Next.js frontend. | Launch a type-safe website with integrated content management. | `const starter='Payload CMS Next.js starter' as const;` |
| 201 | tRPC | tRPC creates end-to-end type-safe APIs by sharing inferred router types between server and client. | Build typed dashboard procedures without generating an API schema. | `const appRouter=router({userById:publicProcedure.input(z.string()).query(({input})=>getUser(input))});` |
| 202 | GraphQL with Apollo | Apollo Client consumes GraphQL APIs with normalized caching, queries, mutations, and React integration. | Connect an interactive Next.js dashboard to a GraphQL backend. | `const client=new ApolloClient({uri:GRAPHQL_URL,cache:new InMemoryCache()});` |
| 203 | Sentry | Sentry captures application errors, traces, releases, and performance telemetry from Next.js runtimes. | Diagnose a production Server Action failure with stack traces. | `Sentry.captureException(error);` |
| 204 | PostHog | PostHog provides product analytics, feature flags, session replay, and experimentation. | Measure onboarding conversion and gradually release a feature. | `posthog.capture('onboarding_completed',{plan});` |
| 205 | Vercel Analytics | Vercel Analytics records privacy-oriented traffic and page-view insights through a Next.js component. | Monitor which application routes receive the most visits. | `const analytics=<Analytics/>;` |
| 206 | Vercel Speed Insights | Speed Insights reports real-user Core Web Vitals and route performance. | Detect a production regression in Largest Contentful Paint. | `const speedInsights=<SpeedInsights/>;` |
| 207 | `next-sitemap` | `next-sitemap` generates sitemaps and robots files from Next.js routes during the build workflow. | Generate large route indexes for a Pages Router content site. | `const config={siteUrl:process.env.SITE_URL!,generateRobotsTxt:true} satisfies IConfig;` |
| 208 | `next-seo` | `next-seo` provides reusable SEO components, especially for Pages Router projects, while App Router commonly uses Metadata. | Maintain standardized social tags in an older application. | `const seo=<NextSeo title="Pricing" description="Compare plans"/>;` |
| 209 | `next-pwa` | `next-pwa` wraps Next.js configuration to generate service-worker and offline behavior, though newer projects may prefer modern alternatives. | Maintain offline caching in an existing Next.js PWA. | `export default withPWA({dest:'public'})(nextConfig);` |
| 210 | `next-themes` | `next-themes` manages client-side themes, system preference detection, and persistent dark-mode selection. | Add flicker-resistant light and dark themes. | `const provider=<ThemeProvider attribute="class" defaultTheme="system" enableSystem>{children}</ThemeProvider>;` |
