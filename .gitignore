# Local tooling / personal config
.commandcode/
.codegraph/

# deps / build
node_modules/
.pnpm-store/
apps/web/test-results/
apps/web/.next/
apps/web/out/
apps/web/dist/
apps/web/tsconfig.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example
!.env.gcp.example
# compose.yaml mounts apps/web/.env.prod; the template is safe to commit (it
# contains only empty placeholders) and without it `docker compose up` fails
# immediately for anyone following the documented path (audit finding R-12).
!.env.prod.example
*.pem
*.key

# payment provider key material (never commit)
xendit-key/

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
