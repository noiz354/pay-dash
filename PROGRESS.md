# Progress — Prototype → Production

Build-status tracker for the payment-gateway dashboard. Prototype screens are done; production migration is tracked in Milestones below.

## Milestones (shipped = ✅, in-progress = 🟡, planned = ⬜)

| Phase | Scope | Status | ADR | Owner | Date |
|---|---|---|---|---|---|
| 0 Scaffold | Next.js App Router + TS + Tailwind + tokens + TEST MODE banner | ✅ | 0001 0002 | | 2026-08-30 |
| 1 Data/Auth/Security | Postgres + Prisma + DAL + headers + Better Auth | ✅ | 0003 0004 |  | 2026-08-31 |
| 2 Payments/Webhooks | `xendit-node` + `/api/webhooks/xendit` verify/dedupe/queue + ledger | ✅ | 0003 |  | 2026-08-31 |
| 3 Observability/Tracking | Sentry + OTEL `instrumentation.ts` + pino + `track()` + Web Vitals | ✅ | 0005 |  | 2026-08-31 |
| 4 i18n/Animation | `next-intl` `app/[locale]` + `motion` | ✅ | 0002 |  | 2026-08-31 |
| 5 3D/Polish | `three` + R3F + drei behind `dynamic(ssr:false)` + `Hero3D` | ✅ | — |  | 2026-08-31 |
| 6 Testing/CI | Vitest + RTL + Playwright + GitHub Actions + `prisma migrate deploy` | ✅ | 0001 |  | 2026-08-31 |
| 7 Flow completeness | Server Actions (create/refund/retry) + URL filter state + toasts/skeletons/empty states + `transactions/[id]` | ✅ | 0006 |  | 2026-09-02 |

> Flip one row per PR that touches `apps/web`. See `AGENTS.md` and `docs/adr/`.

## Screen Migration (33 prototypes → `apps/web` routes)

| Screen | Prototype | Route | Status | Notes |
|---|---|---|---|---|
| Home Overview | `screens/mobile/dashboard_home/` | `app/[locale]/dashboard/page.tsx` | ✅ | Profile greeting, balance strip, URL chart range — 2026-09-02 ADR-0012 |
| API Key Management | `screens/mobile/api_key_management/` | `app/[locale]/settings/api-keys/page.tsx` | ✅ | shadcn 100+ — 2026-08-31 ADR-0002 | No SDK — Dashboard-only (`INTEGRATION.md:313`) |
| Balance History | `screens/mobile/balance_history/` | `app/[locale]/balance/page.tsx` | ✅ | One derived figure; top-up/withdraw dialogs; URL filters — 2026-09-01 ADR-0011 |
| Custom Reports Builder | `screens/mobile/custom_reports_builder/` | `app/[locale]/reports/builder/page.tsx` | ✅ | Real query over store rows + real CSV export — 2026-09-02 ADR-0020 | `Transaction.getAllTransactions()` |
| Customer Directory | `screens/mobile/customer_directory/` | `app/[locale]/customers/page.tsx` | ✅ | `table+avatar+badge+pagination+checkbox` — 2026-08-31 ADR-0002 |
| Developer Settings | `screens/mobile/developer_settings/` | `app/[locale]/settings/developer/page.tsx` | ✅ | shadcn 100+ — 2026-08-31 ADR-0002; webhook card = receive-side truth, `webhookRetries` removed — 2026-09-02 ADR-0015 | Webhooks Dashboard |
| Fraud Prevention Blocklist | `screens/mobile/fraud_prevention_blocklist/` | `app/[locale]/fraud/blocklist/page.tsx` | ✅ | `table+badge+input+button` — 2026-08-31 ADR-0002 |
| Identity Verification (KYC) | `screens/mobile/identity_verification_kyc/` | `app/[locale]/kyc/page.tsx` | ✅ | Profile-derived steps, real upload/submission store, no review-outcome claims — 2026-09-02 ADR-0019 | KYC not in v7 SDK (INTEGRATION.md:93) |
| Payment Links | `screens/mobile/payment_links_invoices/` | `app/[locale]/payments/links/page.tsx` | ✅ | Derived link status; create/close/simulate actions; `[id]` detail — 2026-09-02 ADR-0013 | `Invoice` / `PaymentRequest` |
| Payout Settings | `screens/mobile/payout_settings/` | `app/[locale]/payouts/settings/page.tsx` | ✅ | shadcn 100+ — 2026-08-31 ADR-0002 | `Payout.getPayoutChannels()` |
| Subscription Management | `screens/mobile/subscription_management/` | `app/[locale]/subscriptions/page.tsx` | ✅ | Real plan store + URL-driven query + real create/export — 2026-09-02 ADR-0021 | `Invoice` recurring |
| Team Permissions | `screens/mobile/team_permissions/` | `app/[locale]/team/page.tsx` | ✅ | shadcn 100+ — 2026-08-31 ADR-0002 | No SDK |
| Transaction Ledger | `screens/mobile/transaction_ledger/` | `app/[locale]/transactions/page.tsx` | ✅ | 7-col DataTable DAL #106 — 2026-08-31 ADR-0001/0002 |
| Webhook Logs | `screens/mobile/webhook_logs/` | `app/[locale]/webhooks/page.tsx` | ✅ | Inbound callback log (receive-side); route persists every callback; simulate/replay TEST MODE; `[id]` detail — 2026-09-02 ADR-0014 | Receive-only, persist inbound |
| Dashboard Home | `screens/desktop/dashboard_home_desktop/` | `app/[locale]/dashboard/page.tsx` | ✅ | Shared responsive — 2026-08-31; home-state pass 2026-09-02 ADR-0012 |
| Balance & History | `screens/desktop/balance_history_desktop/` | `app/[locale]/balance/page.tsx` | ✅ | Shared — 2026-08-31; balance pass 2026-09-01 ADR-0011 |
| Billing & Invoices | `screens/desktop/billing_invoices_desktop/` | `app/[locale]/billing/page.tsx` | ✅ | `table+select+tabs+calendar+badge` — 2026-08-31 ADR-0002; `PAYMENT_METHODS` moved to `lib/invoice-status.ts` (client-safe) — 2026-09-02 fix |
| Bulk Payouts | `screens/desktop/bulk_payouts_desktop/` | `app/[locale]/payouts/bulk/page.tsx` | ✅ | `file-upload+progress+card+table` — 2026-08-31 ADR-0002 |
| Custom Reports Builder | `screens/desktop/custom_reports_builder_desktop/` | `app/[locale]/reports/builder/page.tsx` | ✅ | Shared — 2026-09-02 ADR-0020 | Shared |
| Customer Directory | `screens/desktop/customer_directory_desktop/` | `app/[locale]/customers/page.tsx` | ✅ | Shared — 2026-08-31 |
| Detailed Audit Log | `screens/desktop/detailed_audit_log_desktop/` | `app/[locale]/audit/page.tsx` | ✅ | `tabs+select+calendar+checkbox` — 2026-08-31 ADR-0002 |
| Developer Settings | `screens/desktop/developer_settings_desktop/` | `app/[locale]/settings/developer/page.tsx` | ✅ | shadcn 100+ — 2026-08-31 ADR-0002 | Shared |
| Fraud Prevention Console | `screens/desktop/fraud_prevention_desktop/` | `app/[locale]/fraud/page.tsx` | ✅ | `table+tabs+switch+badge` — 2026-08-31 ADR-0002 |
| Identity Verification (KYC) | `screens/desktop/identity_verification_kyc_desktop/` | `app/[locale]/kyc/page.tsx` | ✅ | Shared — 2026-08-31 |
| Merchant Profile Settings | `screens/desktop/merchant_profile_settings_desktop/` | `app/[locale]/settings/merchant/page.tsx` | ✅ | shadcn 100+ — 2026-08-31 ADR-0002; auto-debit switch wired to the billing summary — 2026-09-02 ADR-0018 | No SDK |
| Notification Preferences | `screens/desktop/notification_preferences_desktop/` | `app/[locale]/settings/notifications/page.tsx` | ✅ | shadcn 100+ — 2026-08-31 ADR-0002 | No SDK |
| Risk & Velocity Limits | `screens/desktop/risk_velocity_limits_desktop/` | `app/[locale]/risk/page.tsx` | ✅ | shadcn 100+ — 2026-08-31 ADR-0002 | No SDK |
| Onboarding Checklist | `screens/desktop/sub_merchant_onboarding_checklist_desktop/` | `app/[locale]/onboarding/page.tsx` | ✅ | shadcn 100+ — 2026-08-31 ADR-0002 | `forUserId` partial |
| Subscription Management | `screens/desktop/subscription_management_desktop/` | `app/[locale]/subscriptions/page.tsx` | ✅ | Shared — 2026-09-02 ADR-0021 | Shared |
| Support & Documentation Hub | `screens/desktop/support_documentation_hub_desktop/` | `app/[locale]/support/page.tsx` | ✅ | Real links + `?ref`-aware mailto, zero dead affordances — 2026-09-02 ADR-0016 | Static (INTEGRATION.md) |
| System Health & Webhooks | `screens/desktop/system_health_monitoring_desktop/` | `app/[locale]/system/page.tsx` | ✅ | States only what's measured — inbound 24h counts + recent callbacks; invented metrics/queue/monitoring panel removed — 2026-09-02 ADR-0017 | Webhooks |
| Team & Permissions | `screens/desktop/team_permissions_desktop/` | `app/[locale]/team/page.tsx` | ✅ | shadcn 100+ — 2026-08-31 ADR-0002 | Shared |
| Transaction Ledger | `screens/desktop/transaction_ledger_desktop/` | `app/[locale]/transactions/page.tsx` | ✅ | Shared — 2026-08-31; URL filters + pagination + row links — 2026-09-01 ADR-0006 |
| Transaction Detail | _new (follows ledger rows)_ | `app/[locale]/transactions/[id]/page.tsx` | ✅ | Summary + timeline + refund/retry + not-found — 2026-09-01 ADR-0006 |

Full manifest in `SCREENS.md`.

## Prototype Baseline (shipped)

- Mobile (Kinetic Enterprise): 14 of 14 built — `screens/mobile/*/code.html` + `screen.png`
- Desktop (Kinetic Ledger): 19 of 19 built — `screens/desktop/*_desktop/code.html` + `screen.png`
- Design systems: Kinetic Enterprise + Kinetic Ledger — Complete (`design-system/*/DESIGN.md`)

## Decisions Log

| ADR | Title | Status |
|---|---|---|
| [0001](docs/adr/0001-nextjs-app-router.md) | Next.js App Router + TypeScript + pnpm | Accepted |
| [0002](docs/adr/0002-tailwind-shadcn-tokens.md) | Tailwind + shadcn/ui + Kinetic Tokens | Accepted |
| [0003](docs/adr/0003-postgres-prisma.md) | PostgreSQL + Prisma | Accepted |
| [0004](docs/adr/0004-auth-clerk-vs-betterauth.md) | Auth — Clerk vs Better Auth | Accepted |
| [0005](docs/adr/0005-observability-sentry-otel.md) | Observability — Sentry + OTEL | Accepted |
| [0006](docs/adr/0006-server-actions-transaction-flows.md) | Server Actions + URL state for the transaction journey | Accepted |
| [0007](docs/adr/0007-customer-directory-and-proxy-activation.md) | Customer directory (derived data seam, deep links) + proxy activation | Accepted |
| [0008](docs/adr/0008-billing-invoices-derived-from-ledger.md) | Billing — invoices derived from ledger fees, payment as a first-class action | Accepted |
| [0009](docs/adr/0009-settings-hub-and-persisted-preferences.md) | Settings — hub route, persisted preferences, reveal-once API keys | Accepted |
| [0010](docs/adr/0010-payout-batches-and-gated-disbursement.md) | Payouts — batches own recipients, disbursement as a gated action | Accepted |
| [0011](docs/adr/0011-balance-derived-from-ledger-and-payouts.md) | Balance — one derived figure from the ledger + payout stores | Accepted |
| [0012](docs/adr/0012-dashboard-derived-home-state.md) | Dashboard — the home page states what the stores already know | Accepted |
| [0013](docs/adr/0013-payment-links-derived-status.md) | Payment Links — a real store, and status the merchant never sets | Accepted |
| [0014](docs/adr/0014-webhooks-inbound-callback-log.md) | Webhooks — the log of callbacks the app actually receives | Accepted |
| [0015](docs/adr/0015-developer-settings-webhook-truth.md) | Developer settings — the webhook card tells the receive-side truth | Accepted |
| [0016](docs/adr/0016-support-hub-real-affordances.md) | Support — the hub that knows where you came from | Accepted |
| [0017](docs/adr/0017-system-measured-status.md) | System — the status page states only what's measured | Accepted |
| [0018](docs/adr/0018-merchant-profile-auto-debit-real.md) | Merchant profile — the auto-debit switch is real | Accepted |
| [0019](docs/adr/0019-kyc-submission-the-app-can-own.md) | KYC — submission the app can own | Accepted |
| [0020](docs/adr/0020-report-builder-a-query-the-app-can-run.md) | Report builder — a query the app can run | Accepted |
| [0021](docs/adr/0021-subscriptions-plans-the-app-can-hold.md) | Subscriptions — plans the app can hold | Accepted |
| [0022](docs/adr/0022-team-members-the-dashboard-can-manage.md) | Team — members the dashboard can manage | Accepted |
| [0023](docs/adr/0023-risk-limits-and-rules-the-dashboard-can-enforce.md) | Risk — limits and rules the dashboard can enforce | Accepted |
| [0024](docs/adr/0024-blocklist-one-store-both-fraud-pages-run-on.md) | Blocklist — one store both fraud pages run on | Accepted |
| [0025](docs/adr/0025-onboarding-checklist-derived-from-real-stores.md) | Onboarding — a checklist derived from the stores it lists | Accepted |
| [0026](docs/adr/0026-audit-log-the-event-history-the-app-owns.md) | Audit log — the event history the app actually owns | Accepted |

Template: `docs/adr/TEMPLATE.md`

## Change Log (newest top)

- **2026-09-02** — Console triage hardening (Addy Osmani workflow): `eslint.config.mjs` now `ignores: [".next/", "out/", "dist/", "node_modules/"]` (was linting generated `webpack.js` → 13 `no-assign-module-variable` errors, now 0 errors); `src/proxy.ts` locale-strip rewrite for `/_next/static/*` (`/en/_next/...` → `/_next/...`) + matcher `en/_next|id/_next` — fixes `text/html` MIME 404 on `layout.css`/`webpack.js` (verified `200` on `/_next/static/chunks/webpack.js` + `/api/vitals`); `PAYMENT_METHODS` moved to client-safe `src/lib/invoice-status.ts` (was `"use server"` in `server/actions/invoices.ts` → `map is not a function` in `PayInvoiceDialog`); `src/instrumentation-client.ts` guards `useReportWebVitals` undefined metric (`startTime` crash); new `src/app/api/vitals/route.ts` 204 for `analytics.ts:21` `sendBeacon`; `sentry.client.config.ts` deduped (init only in `instrumentation-client.ts`); `support/page.tsx` `Button nativeButton={false}` for `render={<a>}`; audit `docs/audit/console-triage-2026-09-02.md`; Milestone 7 → ✅. Refs: ADR-0005/0006. Typecheck 0, lint 0 errors, 286 tests pass.
- **2026-09-02** — Audit pass (ADR-0026): `/audit` (Detailed Audit Log) — a pure mockup whose rows contradicted the world: five hard-coded events **all dated 2023-10-24** from off-world actors (`alice.jones@org.com`, `system@ledger.io`, `e.klein@vendor.co` — the world's domain is `@acmecorp.com`) with invented identifiers (`key_prod_892f...`, `batch_77x21`, `txn_9942a`, `wh_endpoint_2`, `session_b44` — none matches any store), under "Showing 1-5 of **12,042** events / Page 1 of **2409**" (12,042 ÷ 5 ≠ 2,409 — the math doesn't hold), with a **User column and an IP Address column no store can fill** (the ledger has no user or IP field — ADR-0024; INTEGRATION.md:111 grounds the screen in `getAllTransactions()` + webhook events only) and every control inert (search, a range select with an unimplemented "Custom Range...", category select, status checkboxes — all uncontrolled — plus a dead Export CSV and a ⌘K hint with no handler). So the log is now **derived, not seeded**: new read-only `server/data/audit.ts` (the ADR-0025 onboarding pattern — it owns no facts) where `getAuditEvents()` unifies the timestamped events the stores already hold — **payments** ← the 46 ledger transactions' `events` timelines (created/authorized/captured/declined/awaiting/refunded, detail carries the amount; 140 events), **payouts** ← the 5 batches' `timeline` (actor = the batch's real `source`; 10), **webhooks** ← the 7 inbound callback-log events (received/deduplicated/rejected with the real reason), **configuration** ← 3 API-key creations + 10 blocklist additions + the deployed velocity ruleset + 5 team joins + 1 team invite (20) — **177 total** (the true count replaces 12,042); `listAuditEvents({q,category,status,range,page})` (10/page newest-first, over-range pages clamp), `auditSummary()`, `auditEventsToCsv()`; client-safe `lib/audit-options.ts`. Honest columns: Timestamp · Category · Action & Resource · Detail · Status — **the User and IP columns are dropped and the page says so** in its subline; status maps from the stores' own `kind` (a rejected callback is a **Warning, not a Failure** — the endpoint doing its job). Page: URL-state filter bar (debounced search, category/status/range 24h-90d/all — "Custom Range..." gone, live "N events" count, Clear filters, **⌘K wired** to focus search), real `TablePagination` ("1 to 10 of 177"), filter-aware EmptyState, **real Export** `/api/exports/audit` (mirrors the URL into the store's single filter implementation, pages through > 100 matches), `metadata`, tokens only. Dropped: the five 2023-10-24 rows, off-world emails, invented ids, "12,042"/"2409", User + IP columns, "Custom Range...", all uncontrolled filters, the dead export, the unbacked ⌘K. Tests: `audit.test.ts` (8), `e2e/audit.spec.ts` (5 serial, incl. filter→URL round trip, deep links, pagination, filtered CSV download), `uat-journeys` F2 rewritten (it was red — it asserted a "Footer"/"Main" tab and `log_001` the page never had); typecheck clean, lint 0 errors, unit green (286), SSR verified ("177 events"/"1 to 10 of 177" pre-rendered, `?category=WEBHOOKS` → "7 events"/"1 to 7 of 7", export → 177 rows all / 7 filtered with the right header; no "12,042"/"2409"/"2023"/"@org.com"/"key_prod_892f"/IP column). Manual procedure in `docs/audit/audit-log-test-procedure-2026-09-02.md`.
- **2026-09-02** — Onboarding pass (ADR-0025): `/onboarding` (Sub-Merchant Onboarding) — a pure mockup whose numbers the app's own world contradicted: hard-coded "3 of 4 sections completed · **75%**" that could never change; a Technical card that struck through its own completed items (`line-through` on "API Keys Generated" / "Webhook Endpoints Configured") while "First Test Transaction" sat unchecked — even though the ledger holds **33 SUCCEEDED** transactions, 3 API keys exist and the webhook callback log has 7 events; an invented "Operating Account ****4592 / Verified on Oct 24" (the real accounts are `**** 1234` / `**** 8891` / `**** 4420`, two verified); compliance claims the app cannot own ("Articles of Inc. — Verified by system", "W-9 — Signed & uploaded" — document onboarding is the Xendit Platform API, not in the v7 SDK, INTEGRATION.md:330–339); the "Accounts" breadcrumb link-styled but not a link; all four card CTAs dead; no `metadata`; a raw `rgba(0,63,177,0.1)` glow. So the checklist is now **derived, not seeded**: new read-only `server/data/onboarding.ts` (the ADR-0011 balance pattern applied to a checklist — it owns no facts) where `getOnboardingStatus()` resolves each section against its owner — Business Profile ← merchant profile (legal name/DBA, address, tax ID); Bank Setup ← payout bank accounts + destination ("Bank Central Asia · **** 1234 (default)", "2 of 3 accounts verified"); Technical Setup ← `listApiKeys()` + the webhooks callback log + SUCCEEDED ledger rows ("3 keys on file · 2 live, 1 sandbox", "7 callback events received", "33 successful transactions settled"); Compliance ← KYC completeness + submission (deliberately unseeded store). **The compliance ruling**: the section is shown but NEVER counted and NEVER COMPLETED — the app can hold "submitted" (ADR-0019) but not "approved", so its badge is ACTION REQUIRED until a document is submitted, then REVIEW PENDING (unit-tested invariant); the progress bar tracks the three app-owned sections only → seeded world **3 of 3 · 100%**, a true statement for a merchant with 33 succeeded payments, with a plain footnote that the compliance review is shown but not counted. Page: `metadata`, real merchant name in the subtitle ("Acme Corp Setup" gone), derived progress bar (`aria-valuenow`), shared `OnboardingCard` (no `line-through` — that visual is dropped; attention border moves to whichever section needs action — in the seeded world, compliance; raw rgba glow → tokens only), the compliance-team note on the compliance card, and every CTA a real locale-aware link to the owner: Accounts (breadcrumb) / Review Details → `/settings/merchant`, View Documents → `/kyc`, Manage Accounts → `/payouts/settings`, Go to Developer Dashboard → `/settings/developer`. Dropped: 75% / "3 of 4", "Acme Corp Setup", `****4592`, "Oct 24", "Verified by system", "Signed & uploaded", struck-through completed items, all four dead buttons. Tests: `onboarding.test.ts` (7), `e2e/onboarding.spec.ts` (5 serial, incl. the compliance never-claimed state and all four CTA destinations), `uat-journeys` F8 + J4 rewritten (both were red — F8 asserted "2 of 3 completed" + an "Add directors" collapsible + a `Documents` checkbox, J4 asserted the same checkbox — none of it existed on the page); typecheck clean, lint 0 errors, unit green (278), SSR verified (all 15 derived strings pre-rendered incl. "3 of 3 sections completed" / "100%" / the four real CTAs; no "75%"/"4592"/"Oct 24"/"Acme Corp Setup"/"Verified by system"/"Signed & uploaded"/`line-through`). Manual procedure in `docs/audit/onboarding-test-procedure-2026-09-02.md`.
- **2026-09-02** — Fraud/blocklist pass (ADR-0024): `/fraud` + `/fraud/blocklist` — two mockups of the same concept that disagreed with each other: each route hard-coded its OWN blocklist in module scope (different IP values, October **2023** dates — 192.168.1.105/10.0.0.24/172.16.254.1/45.33.22.110 vs 192.168.1.1/203.0.113.42/198.51.100.7/45.22.19.102), plus invented metrics (Total Blocked Entities **14,209** "+12% this week", High Risk IPs 8,432 — a concept with no data source, the ledger has no IP field —, Blocked Cards 3,194), Card/Email tabs rendered prose ("No card blocklist entries."), Add/Filter/Export/⋮/delete all inert, "Showing 1-4 of **124**" with a live Next button, `bg-white` + emerald/red literals, no `metadata`, no model (INTEGRATION.md:92/:113/:319: "— none; Fraud rules are Dashboard/console-only"). Now there is exactly **one store**: `server/data/blocklist.ts` — 10 deliberately seeded, date-relative entries consolidating both prototype lists (6 IPs deduped, 2 **masked cards** `453322 •••• 0110` first-6/last-4, 2 disposable email domains; `blk_`+djb2 ids; reason vocab KNOWN_MALICIOUS/HIGH_FREQUENCY/CHARGEBACK_ABUSE/MANUAL_ENTRY) + `listBlocklist` (type/q/page, newest-first) + `blocklistSummary` (per-type counts + 30d adds) + `addBlocklist` (per-type validation: IPv4/IPv6 octet-checked, cards 12–19 raw digits → stored masked, email domains not full addresses, duplicate rejection) + `removeBlocklist` + CSV; client-safe `lib/blocklist-options.ts`. `/fraud`: metric cards = the 3 derived entry types (real "8 added in the last 30 days"; invented numbers/deltas + "High Risk IPs" dropped), panel with URL tabs (lowercase `?type=ip|card|email`, live counts on labels), debounced search, **real Export** `/api/exports/blocklist` (mirrors URL filters incl. lowercase tab normalisation, shared ExportCsvButton), real ⋮ Remove + shared **Add dialog** (`useActionState`, per-type validation, success view), a subline link to the blocklist route; `/fraud/blocklist`: the focused view over the SAME store (an add/remove is visible on both routes — the contradiction is structurally impossible); shared `BlocklistPanel` pre-renders all three sections (base-ui mounts only the active panel; the rest ride in the RSC payload — no tab flash); real TablePagination ("1 to 6 of 6" — the 124 dropped). Dropped: both 2023 row sets, invented metrics/deltas, "of 124" + dead Next, prose empty tabs, literal colors; `metadata` on both. Tests: `blocklist.test.ts` (7), `e2e/blocklist.spec.ts` (5 serial, incl. the add-on-one-page-see-it-on-the-other round trip, URL tabs/deep links, remove, filtered CSV download), `uat-journeys` E1+E2 rewritten (both were red — E1 asserted a "Blocklist — see /fraud/blocklist" text + switch, E2 asserted "4111 1111 1111 1111" — neither existed); typecheck clean, lint 0 errors, unit green (271), SSR verified (10 entries pre-rendered in the RSC payload, tab deep links, no "14,209"/"of 124"/"2023"/`bg-white`). Manual procedure in `docs/audit/blocklist-test-procedure-2026-09-02.md`.
- **2026-09-02** — Risk pass (ADR-0023): `/risk` (Risk & Velocity Limits) — a pure mockup its own title couldn't back up: an invented "Alerts (24h): 14, 12% vs yesterday" with no alert model, two hard-coded triggers ("Velocity Max: Card ending 4492" — the ledger has no card last4 field — and "Txn Limit Approaching: Merchant A" with static "10:42 AM/09:15 AM"), **USD caps in an IDR app** ($1.5M/$45M uncontrolled inputs), "Discard Draft"/"Deploy Changes" buttons with no draft state behind them, a static "Active Ruleset" badge, "View All" with no destination, the prototype's literally-empty right column, no `metadata` — and no risk model anywhere (INTEGRATION.md:117/:320: "— none; Velocity/risk thresholds are Dashboard-only"). So the app now owns its ruleset and derives the rest: new `server/data/risk.ts` — **seeded** 4-rule velocity ruleset (card velocity 25/hourly ALERT, max daily card charge Rp 50.000.000 BLOCK, customer burst 10/hourly ALERT, high-value txn Rp 100.000.000 disabled-by-default) + **IDR** volume caps (daily Rp 2.000.000.000 / monthly Rp 60.000.000.000, sized against the real world) + "deployed 12 days ago" baseline; **derived** high-risk alerts (every ledger transaction with riskScore >= 60 — `HIGH_RISK_SCORE`/`VOLUME_ALERT_PCT` in client-safe `lib/risk-options.ts` — 11 in the seeded ledger, each deep-linking to `/transactions/[id]`), a daily-cap breach alert (≥75% usage, untriggered by the real 24h volume — both branches unit-tested), cap usage (24h/30d settled ÷ deployed caps) and the risk-score distribution (0–39/40–59/≥60); real **draft/deploy workflow** (`patchDraft`/`deployRiskSettings`/`discardDraft`; badge derived Active Ruleset ↔ Draft pending; header buttons enabled exactly when a draft exists). Page: derived alert bento (count + "N of M" top-5, per-alert transaction links — dead "View All" dropped), volume card (optimistic switch drafts immediately, `Rp` inputs server-validated via parseAmount: > 0, monthly ≥ daily, "Save to draft", derived usage lines per field), real rules table (scope/threshold/window + Alert/Block chips + per-row draft switch), right column = Ledger Risk Profile (distribution + deployed-at); `metadata`, tokens only. Dropped: "14"/"12%", the ghost triggers, USD framing, static badge, empty spacer. Tests: `risk.test.ts` (5), `e2e/risk.spec.ts` (4 serial, incl. full draft→deploy→discard round trip, alert deep link, server-side validation), `uat-journeys` E4 rewritten (it was red — written against an older slider/#block iteration that no longer exists); typecheck clean, lint 0 errors, unit green (264), SSR verified (11 high-risk / 5 links / "46 transactions scanned"; no "Card ending 4492"/"Merchant A"/"USD"/"View All"/"12% vs yesterday"). Manual procedure in `docs/audit/risk-test-procedure-2026-09-02.md`.
- **2026-09-02** — Team pass (ADR-0022): `/team` — a pure mockup with an internal contradiction: four hard-coded rows (three of them off-world `@ledger.com`/`@ledgerscale.io` addresses in module scope), an "Invited" member on the Members tab while the Pending Invites tab said "no pending invitations", a dead config surface (search, Filter, Export, Add Member, select-all, the permanently-disabled "0 selected" bulk bar, every ⋮, pagination), an invented "Showing 1 to 4 of 24", placeholder Roles/Pending Invites tabs, static "last active" strings, `bg-white`/emerald/amber literals, no `metadata` — and no team model anywhere (INTEGRATION.md:97/:122/:318: no Xendit source; "Role-based access control is Dashboard-only"). So the app now owns its roster: new `server/data/team.ts` — **6 seeded members on the merchant's own `@acmecorp.com` domain** (5 active: Daniel Wirawan · Admin, Michael Chen · Developer, Sarah Anderson · Analyst, Priya Nair · Developer, Kevin Halim · Analyst; 1 invite: Elena Jenkins · Risk Analyst, `mem_`+djb2 ids, `statuses` filter so Members tab = ACTIVE+DEACTIVATED and Pending Invites = INVITED — the contradiction is structurally impossible) + `listMembers` (q/role/statuses/page) + `inviteMember` (lands INVITED) + `changeMemberRole`/`deactivateMember`/`reactivateMember` (join date set on reactivation) + `resendInvite`/`revokeInvite` (7-day TTL stated on the page) + `roleCatalog()` (derived active counts) + `membersToCsv` (8 cols, raw values); client-safe `lib/team-roles.ts` (4 roles w/ descriptions + permissions, status labels, `INVITE_TTL_DAYS` — shared by page, dialog, chips, e2e); server actions in `server/actions/team.ts` (`useActionState` pairs, `ids[]` for bulk-capable + `id` for singles). Page: searchParams-driven Members tab (debounced search, role filter, derived "N members", real TablePagination "1 to 5 of 5", filter-aware EmptyState) + real rows (initials avatars, `formatRelative` over real timestamps, token chips) + **real selection as state** (`Set`, cleared on mutation; bulk bar drives real Change Role single/bulk + Deactivate) + real ⋮ menus (Change-role sub-menu, Deactivate/Reactivate per status) + Roles tab (catalog + derived counts) + Pending Invites tab (derived, Resend/Revoke, true empty state) + **Add Member dialog** (`useActionState`, server validation, success view) + **real export** `/api/exports/team` (mirrors URL filters, shared ExportCsvButton). Dropped: off-world addresses, "24", unreachable bulk bar, placeholder tabs, static last-active, literal colors; `metadata`. Tests: `team.test.ts` (7), `e2e/team.spec.ts` (7 serial, incl. invite→pending round trip, revoke/resend, bulk role+deactivate+reactivate, CSV download), `uat-journeys` F1 rewritten (it was red — it asserted an "Invite" button, a 2FA `button[role=switch]` and a "⋯" Remove menu the page never had); typecheck clean, lint 0 errors, unit green (259), SSR verified (5 members, "1 to 5 of 5", no `ledger.com`/`bg-white`/`24`/"No pending invitations"; `/api/exports/team` → 6 rows, `?role=DEVELOPER` → 2). Manual procedure in `docs/audit/team-test-procedure-2026-09-02.md`.
- **2026-09-02** — Subscriptions pass (ADR-0021): `/subscriptions` — the app's last pure mockup: three invented stat cards (the same "1,248" the reports builder faked, plus a "1,290" table total contradicting them, "+12% vs last month" and "−3 recovered this week" deltas), three hard-coded rows (2023 dates, unformatted amounts, "Past Due" — not an app status — and two third-party `lh3.googleusercontent.com` avatars), and a config surface in which search, Filter, Export, Create Subscription, every ⋮ and every pagination button had no handler. The app owned no customer-plan model anywhere, and the documented source (INTEGRATION.md:96/:119 — `getInvoices()` with recurring filters) has no target: the Invoice model is platform billing with no recurring dimension. So the app now holds its plans: new `server/data/subscriptions.ts` — **10 seeded plans tied to real customer-directory customers** (ids via the same `customerIdFromEmail` pure hash, so "View customer" always resolves; deliberately seeded — a plan is a business record the app would have generated, the class the ADR-0019 KYC distinction draws, and the ledger's own "Subscription renewal — Growth plan" description shows the fiction includes recurring billing) + `listSubscriptions` (customer-directory pattern) + `createSubscription` (lands PENDING_SETUP — the app never claims a plan is live the moment it is made) + `subscriptionSummary` (derived cards: active count + MRR = monthly face + yearly ÷ 12) + client-safe `lib/subscription-csv.ts`. The page is searchParams-driven (`q`/`status`/`page` like /customers): real debounced search, real status filter, real TablePagination; derived stat cards (no invented deltas); real rows (initials avatars, `Rp …`, token chips, started + next billing); ⋮ = real dropdown (View customer → /customers/[id], View payments → /transactions?q=email, Copy plan ID); **real export** via the app's `/api/exports/subscriptions` convention (mirrors URL filters + shared ExportCsvButton); **real create dialog** (customer picked from the real directory, plan name, IDR amount, interval; server-validated; success view with the new id; list re-renders). Dropped: invented stats/trends, hard-coded rows, external avatars, 2023 dates, dead controls; `metadata`. Tests: `subscriptions.test.ts` (7), `e2e/subscriptions.spec.ts` (5 serial, incl. create round trip + filtered CSV download), `uat-journeys` F7 rewritten (it was red — it asserted a "Budi" customer + Calendar/Subscriptions tabs the page never had; no absolute count since a prior spec may create a plan in the shared store); typecheck clean, lint 0 errors, unit green, SSR verified (MRR Rp 94.550.000 card; no `1,248`/`1,290`/`TechFlow`/external avatars; `/api/exports/subscriptions` returns the CSV). Manual procedure in `docs/audit/subscriptions-test-procedure-2026-09-02.md`.
- **2026-09-02** — Reports builder pass (ADR-0020): `/reports/builder` — the app's last pure mockup: five hard-coded preview rows (ids `txn_1Nj8V2…` absent from the store, "Oct 24" dates, four emails absent from the customer store, corrupted USD amounts under an "Amount (USD)" header in an IDR app), a fabricated "Showing 1,248 rows / 1-50 of 1,248" (actual ledger: 46 rows), and a config column in which every control — data source, dates, 7D/30D/YTD, filter Add/×, columns, Apply, Reset, Schedule, Export CSV, pagination — had no handler. Unlike KYC there is no SDK constraint (INTEGRATION.md:89/:109 documents the screen as `Transaction.getAllTransactions({filters})`), so the page now runs a real query over the app's own rows: `lib/report-options.ts` (client-safe — per-source columns, the real status vocabularies, row mappers, `runQuery`, `buildReportCsv` with RFC-style quoting) + a client `<ReportBuilder>` fed the full row sets by the server page (46 ledger rows / 5 batches / 11 customers, deterministic seed `mulberry32(20260901)`); Data Source **Transactions/Payouts/Customers** (Disputes dropped — no model exists; footnote: refund facts live in the ledger as status REFUNDED); real date range (presets fill the inputs), real status + IDR amount filters, column checkboxes that drive both preview and CSV, Reset, live query (no Apply); preview = real rows with `Rp` amounts, real emails/methods, status chips, id cells linking to `/transactions/[id]` / `/payouts/[id]` / `/customers/[id]`, `EmptyState` on zero matches; **Export CSV actually downloads** `kinetic-<source>-<date>.csv` (client Blob, exactly the filtered rows + selected columns, raw values, disabled when empty); Schedule + fake pagination dropped; `bg-white`/`bg-amber-50` → tokens; `metadata`. The support page's "Reporting & Export → /reports/builder" deep-link (ADR-0016) now lands on a working page. Tests: `lib/report-options.test.ts` (10), `e2e/reports.spec.ts` (6 serial, incl. a real download assertion), `uat-journeys` F3 rewritten (it was red against the prototype — it asserted a QRIS checkbox and `IDR 1,000,000.00` that never existed); typecheck clean, lint 0 errors, unit green (245), SSR verified (real ids + `Rp` amounts; no `1,248`/`txn_1Nj8V2`/`Amount (USD)`/`Schedule`/`bg-white`). Manual procedure in `docs/audit/reports-test-procedure-2026-09-02.md`.
- **2026-09-02** — KYC pass (ADR-0019): `/kyc` — hard-coded 4-step progress, a hard-coded `acme_corp_incorporation_2023.pdf` attachment with a no-op Delete, a file input with no `onChange`, an uncontrolled select (and a "Delaware, USA" placeholder in an IDR app), and handler-less Save Draft / Submit Step buttons — rebuilt around what the app can truthfully own, given the documented constraint that KYC is **not in the v7 node SDK** (INTEGRATION.md:93/:323): new `server/data/kyc.ts` (one submission record: fileName/sizeBytes/docType/jurisdiction/submittedAt via `submitKycDocument`/`removeKycDocument`; **deliberately unseeded** — a compliance document is an unverified claim the app won't fabricate) + `profileKycCompleteness()` so step 1 derives from the merchant profile (seeded → done with real values; blank a field → listed as missing, "Edit profile" door); the rail keeps only data-backed steps (Basic Info → Business Documents → Submission; "Beneficial Owners" removed, compliance-side footnote citing the SDK note); `kyc-upload.tsx` — real file input with the stated limits enforced (`lib/kyc-options.ts`: PDF/JPEG/PNG, 10 MB), working Remove (server action), one real **Submit for review** ("Save Draft" removed — a draft store would invent more than the app knows); derived badge (Action required `--warning` → Awaiting review `--pending-status` + date); the AML banner drops the unverifiable "end-to-end encrypted/banking-grade" claims and states the review boundary on-page; `bg-white`/amber/emerald → status tokens; `metadata`; `formatBytes` added to `lib/format.ts`. Tests: `kyc.test.ts` (4), `e2e/kyc.spec.ts` (3, serial: invented artifacts absent, limit enforcement, upload→submit→review→reload→remove round trip); typecheck clean, lint 0 errors, unit green, SSR verified. Manual procedure in `docs/audit/kyc-test-procedure-2026-09-02.md`.
- **2026-09-02** — Merchant profile pass (ADR-0018, small): the `/settings/merchant` "Auto-debit platform invoices" switch persisted but changed nothing — `getBillingSummary()` hardcoded `autoDebitEnabled: true`, so the billing card said "Auto-debit scheduled" regardless and its "Auto-debit off — set it up" branch was unreachable dead code. The summary now reads `profile.autoDebit` from `getMerchantProfile()` (the established cross-store pattern — balance←payouts, links←ledger, now billing←merchant profile); no UI change (the card already renders both branches and links to the profile page). Everything else on the page verified clean: real form action, dirty-gated Save/Cancel, inline zod errors, live brand swatch, `beforeunload` guard, and `dba`/`legalName` genuinely drive the dashboard greeting (ADR-0012). Tests: `invoices.test.ts` +1 (seed true → toggle false → back), `e2e/billing.spec.ts` +1 round-trip (card → flip switch → save → card follows → restored); typecheck clean, lint 0 errors, unit green. Manual procedure in `docs/audit/merchant-auto-debit-test-procedure-2026-09-02.md`.
- **2026-09-02** — System pass (ADR-0017): `/system` — the app's most fiction-dense page and the target of `/support`'s status pointer — now states only what it measures: inbound webhook flow. Gone: the three invented infrastructure cards (99.99% uptime / 42ms latency / 15% DB capacity / a **142-deep webhook queue** — QUEUES.md: no queue exists, the route processes inline), the "Webhook Delivery Traffic (24h)" chart, the "Recent Webhook Deliveries" table (merchant-URL rows — `api.merchant.com`, `hooks.erp-system.net`, `hooks.legacy-app.io` — the outbound fiction ADR-0014 removed, with four handler-less Inspect buttons and a dead search), the green "All Systems Operational" badge, the **Monitoring Settings panel** (sliders + `ops-alerts@kinetic.local`/`#alerts-critical` channels + a Save button that ran `e.preventDefault()` — `system-form.tsx` deleted) and the Pro Tip card. In its place: `getSystemWebhookSummary()` (server/data/webhooks.ts — last-24h counts by outcome, five most recent callbacks, newest timestamp; seeds are already now-relative offsets so window membership is deterministic: 1 Received + 1 Duplicated in-window) → three 24h count cards in the pill colour language, a "Most recent callbacks" list whose rows route to `/webhooks/[id]` + "View full log" → `/webhooks`, a "What this page measures" card (inbound flow — measured; uptime/latency/DB — not measured, no APM, refuses invented numbers; queue depth — zero by design) with doors to `/settings/developer` and `/settings/notifications` (the app's real notification machinery, ADR-0009), and a "Last callback 2h ago" header chip. `e2e/uat-journeys.spec.ts` F4 (old "System Health" title) was already stale; it joins the known C4/D5/K2/F4 rewrite owed to a later small fix. Tests: `webhooks.test.ts` +3 (deterministic 24h counts, recent list, live rejection), `e2e/system.spec.ts` (4); typecheck clean, lint 0 errors, unit green, SSR verified (16 removed strings absent, zero `href="#"`). Manual procedure in `docs/audit/system-test-procedure-2026-09-02.md`.
- **2026-09-02** — Support pass (ADR-0016): `/support` — the app's last raw prototype, and the destination of two "Report issue" deep-links (`/transactions` row action + transaction detail, both with `?ref=`) — now makes every affordance real without inventing a knowledge base (INTEGRATION.md fixes the screen as "Static content; no API"). The four `href="#"` topic cards route into the pages that actually cover them (API Reference → `/settings/api-keys`, Settlement Guide → `/payouts`, KYC Requirements → `/kyc`, Reporting & Export → `/reports/builder`, descriptions reworded to match); the dropped `?ref` is honoured — a "You're reporting on `<ref>`" banner plus the support email's subject pre-filled (`Kinetic Ledger — issue with txn_…`) via `supportMailto(ref?)` in `lib/support.ts` (client-safe, unit-tested) so the report arrives with its context; the invented three-row "all Operational" status widget becomes one honest pointer → `/system` (whose own metrics are a separately-queued pass); contact is one real action — `mailto:support@kinetic.test` (single documented constant `SUPPORT_EMAIL`, same domain family as `pay.kinetic.test`) — with Live Chat ("Available" — no capability) and Ticket History (no store) removed rather than kept as labelled no-ops; dead search bar + Cmd+K kbd removed; `max-w-5xl` normalized to `max-w-container-max`; `metadata` added. Net: zero dead affordances (was seven — four `href="#"` cards, one `href="#"` status link, three no-op buttons). Tests: `src/lib/support.test.ts` (3), `e2e/support.spec.ts` (5: no dead links/buttons/search, card destinations, status → /system, mailto with and without `?ref`); typecheck clean, lint 0 errors, unit green, SSR verified (banner + encoded subject). Manual procedure in `docs/audit/support-test-procedure-2026-09-02.md`.
- **2026-09-02** — Developer-settings webhook pass (ADR-0015): the `/settings/developer` page — the configuration surface the `/webhooks` config card points at — still told the inverted story ADR-0014 just removed: a "Webhook Endpoints" card with two hard-coded **delivery** endpoints (`api.acme.com` Active / `staging.acme.com` "Failing" — URLs no store, field or route knows; this app receives, it never delivers, INTEGRATION.md :99/:307) and a "Webhook retries" toggle ("retry failed deliveries with exponential backoff") whose `webhookRetries` field nothing consumes. The card is replaced by a **Webhook Endpoint** card stating the receive-side truth from the same sources as the /webhooks config card: the real endpoint URL from `headers().host` (copyable), the token's presence — "Token configured (value hidden)" / "No token set — dev accepts without verification", value never rendered — with the env-var remediation note (`XENDIT_WEBHOOK_TOKEN`; production fails hard without it), the handled event types (`KNOWN_WEBHOOK_EVENTS` + "…stored as unhandled"), and retries as a *fact* sentence (provider re-delivers on non-2xx; endpoint answers 200 fast and dedupes by event id — ADR-0014). `webhookRetries` is deleted end-to-end (DeveloperSettings field + seed, `setDeveloperToggle` union, `DevToggleSchema` enum, action message branch, `DeveloperToggle` prop type, the store-test assertion) — a false control is removed, not re-skinned; the Environment card keeps the sandbox toggle alone. "Open webhook console" → **Open webhook log**; token pills use the exact ADR-0014 strings so the two surfaces can't drift. Tests: `settings.test.ts` updated, `e2e/settings.spec.ts` gains "the webhook section states the receive-side truth" (no acme URLs, no retries switch, endpoint + pill + event chip visible, link to /webhooks); typecheck clean, lint 0 errors, unit green, SSR verified (both token states). Manual procedure in `docs/audit/settings-developer-test-procedure-2026-09-02.md`.
- **2026-09-02** — Webhooks pass (ADR-0014): `/[locale]/webhooks` rebuilt as the log of callbacks the app **receives** — the prototype's outbound-delivery fiction (rows delivering to `api.merchant.com/webhooks/stripe` with 200/500 and "24ms" latency, `1,024 results`, dead filters, `bg-white` literals) is gone, because INTEGRATION.md §7 (:99/:307) fixes the direction: this app receives, it does not deliver. `server/data/webhooks.ts`: `WebhookEvent` store (`__kineticWebhooksStore`, 7 seeded callbacks — both money events reference real ledger `referenceId`s; one seeded retry; one unknown type flagged unhandled; both refusal paths with reasons) where **status is a stored receive-time fact** (RECEIVED/DUPLICATED/REJECTED — the one deliberate departure from derive-everything: a log row is append-only history), `listWebhooks` (q over event id/type/payload, `?status`, `?type` with `unknown` = unhandled, 10/page), `recordInbound` (dedupe by `eventId` → DUPLICATED row with first-received reference) and `rejectInbound` (REJECTED + raw body). The xendit route persists **before it answers** (401/400/422 → REJECTED rows, 200 → RECEIVED/DUPLICATED) through that shared pipeline — the anonymous `webhookSeen` set is gone and the QUEUES.md verification step ("replay twice → second deduped") is now observable; the route's public contract (status codes, `{received, deduped}`, 200-fast, GET) is byte-identical and `processWebhookAsync` stays a documented no-op (ledger processing is the next phase, not this pass). `server/actions/webhooks.ts`: `simulateWebhookAction` (`?simulate=1` dialog incl. the deliberately-unknown `invoice.issued`) and `replayWebhookAction` (detail page; re-POSTs the same event id → visible DUPLICATED row) — TEST MODE standing in for the provider, same relationship as simulate-payment (ADR-0013). Components `components/webhooks/`: `<WebhookStatusPill/>` (received=success, duplicated=pending, rejected=failed), `<WebhookFilters/>` (URL q 350 ms debounce + status + type + clear + count), `<WebhooksTable/>` (ClickableRows to `/webhooks/[id]`, unhandled badge, both empty states, `TablePagination`), `<WebhookConfigCard/>` (server: endpoint URL from `headers().host` + copy, token **configured-or-not** — value never rendered — handled event types, link to `/settings/developer` for retry policy/IP allowlist), `<SimulateWebhookDialog/>` (success panel with `evt_…` id + View event), `<ReplayWebhookButton/>` (toast). New `/webhooks/[id]` detail (breadcrumb, event id + pill + copy, payload JSON or raw body with copy, status note explaining what the provider will do next; Replay hidden for rejections) with `loading`/`not-found`; list `loading`. The setup step "Enable Webhooks" is retitled **Monitor Webhooks** (`lib/setup-steps.ts`). Tests: `src/server/data/webhooks.test.ts` (15), `route.test.ts` (7, `@vitest-environment node` — t3-env's server-var guard rejects jsdom), `route.auth.test.ts` (3, token stubbed before module load), `webhook-status-pill.test.tsx` (2), `e2e/webhooks.spec.ts` (8, serial); SSR + real-HTTP verified (curl POST → 200, replay → 200 deduped, bad JSON → 400, all visible in the log). Manual procedure in `docs/audit/webhooks-test-procedure-2026-09-02.md`.
- **2026-09-02** — Payment Links pass (ADR-0013): `/[locale]/payments/links` rebuilt on a real store — the prototype's four hard-coded rows (incl. the literal `",250.00"` amount), the `href="#"` tabs, the dead filter icon and the "24 results" footer with no page 2 are gone. `server/data/links.ts`: `PaymentLink` store (`__kineticLinksStore`, 8 seeded links) with **derived status** — `deriveLinkStatus` precedence *cancelled → paid → expired → open*, where "paid" is `paidAt` **or** a SUCCEEDED ledger row referencing the link id, so the ledger is the thing that flips a link; `listLinks` (kind/status/q filters, 10/page), `createLink`, `expireLink` (guards: unknown/closed/paid), `recordLinkPayment` (TEST MODE: SUCCEEDED ledger tx with `id = referenceId = link id`, immediate capture, `paidAt` set — the balance, ledger and dashboard all revalidate). `server/actions/links.ts`: `createPaymentLinkAction` (kind-conditional zod via `superRefine`: single ≥ Rp 10,000; multiple 2–20 items ≥ Rp 1,000 each), `expirePaymentLinkAction`, `payPaymentLinkAction`. Client-safe `lib/link-status.ts` (status/kind vocab + `shareUrlOf`). Components `components/links/`: `<LinkStatusPill/>`, `<LinksKindTabs/>` (`?kind=multiple`; single is parameter-free), `<LinksFilters/>` (URL `q` debounce 350 ms + `?status` + clear + count), `<LinksTable/>` (ClickableRows, both empty states, `TablePagination`), `<CreateLinkDialog/>` (`?new=1`, single amount vs. add/remove line items with running total, success panel with id + checkout URL copy), `<ExpireLinkButton/>`, `<SimulatePaymentButton/>` (toasts + View). New `/payments/links/[id]` detail (breadcrumb, pill + copy, checkout URL card, items + total, state-gated actions, status note; **View payment** only where a ledger row exists — seeded pre-window payments deliberately have none) with `loading`/`not-found`; list `loading`. The dashboard setup step "Configure Routing Rules" is retitled **Create a Payment Link** (the page never configured routing; `lib/setup-steps.ts`). Tests: `src/server/data/links.test.ts` (21), `link-status-pill.test.tsx` (2), `e2e/links.spec.ts` (10, serial); manual procedure in `docs/audit/links-test-procedure-2026-09-02.md`.
- **2026-09-02** — Dashboard home-state pass (ADR-0012): `/[locale]/dashboard` now states only what its stores back — greeting from `getMerchantProfile()` via `merchantGreeting()` (the hard-coded "Sarah" persona, which no data model field supports, is gone; `<DashboardHeader/>`); balance strip `<BalanceStrip/>` composed from the same `getBalanceOverview()` as `/balance` (available + pending clearance + reserved + last payout + door to the page); analytics chart takes a URL range (`?range=7d|30d|90d`, keyed `<Suspense>` skeleton on round-trips) with `Total/Succeeded/Failed` series chips (last-series guard), the prototype's static "Oct 18–24" `defaultData` deleted and `data` made required, formatters currency-aware; "Connect Bank Account" setup step **derived** from the verified destination payout account (`resolveSetupSteps`/`nextSetupStep` in `lib/setup-steps.ts`, locked tick with `Linked · **** 1234` badge, next-step CTA skips derived-done); quick actions matched to intent (`Add Customer` → `/customers?new=1` opens the dialog; `Create Invoice` relabelled **Invoices** since ADR-0008 rejects invoice authoring). Tests: `src/lib/setup-steps.test.ts`, `merchantGreeting` cases in `src/lib/settings-options.test.ts`, `src/server/data/transactions.test.ts` (series shape/conservation/empty), `e2e/dashboard.spec.ts`; manual procedure in `docs/audit/dashboard-test-procedure-2026-09-02.md`.
- **2026-09-01** — Balance pass of the flow audit: `/[locale]/balance` rebuilt around one derived figure (`Rp 2.212.783.280` on the seeds) — the contradictory `Rp 1.240.500.000` / `IDR 1.005.870.599` prototype literals and the fake `Daily → BCA ****4910` card are gone; `server/data/balance.ts` derives every movement from the transaction ledger (settlements, pending clearance, refunds), the payout batches (pending = reservation at `batch.createdAt`, paid = settled, failed = FAILED with the partner's reason) and a top-up store, so the page can never disagree with `/payouts`; `server/actions/balance.ts` — `topUpBalanceAction` (TEST MODE instant credit) and `withdrawBalanceAction` (routes into the payout batch flow: single-recipient batch released through the gated settlement, rejection returns the `batchId` with no money moved); the Auto-Withdrawal card reads `getPayoutSettings()` + `getDestinationAccount()` (`Weekly · Friday`, `**** 1234`, `nextRunForCadence` next run) and its switch is a real server action (`toggleAutoWithdrawalAction`); components `<BalanceCards/>`, `<AutoWithdrawalCard/>`, `<AutoWithdrawalToggle/>`, `<BalanceTrendChart/>` (30-day recharts replacing the decorative blur), `<MovementsFilters/>` (URL `type`/`status`/`range`/`q` + debounce), `<MovementsTable/>` (10/page, rows link to `/transactions/txn_…` or `/payouts/BATCH-…`), `<MovementStatusPill/>`, `<TopUpDialog/>` (`?topup=1`), `<WithdrawDialog/>` (`?withdraw=1`, unverified accounts disabled); client-safe `lib/balance-status.ts` (incl. `TOPUP_METHODS` shared by the zod enum and the dialog); export route `/api/exports/balance` (honours the current filters); `loading`/`not-found`. Tests: `src/server/data/balance.test.ts` (16), `src/lib/payout-status.test.ts`, `src/components/balance/movement-status-pill.test.tsx`, `src/components/balance/balance-dialogs.test.tsx` (FormData contract for both dialogs), `e2e/balance.spec.ts`; manual procedure in `docs/audit/balance-test-procedure-2026-09-01.md`; ADR-0011.
- **2026-09-01** — Payouts pass of the flow audit: new `/[locale]/payouts` index (previously 404) with derived summary cards, URL filters (`q`, `status`, `range`, `sort`, `page`), batch history table and clickable rows, plus a `/payouts` sidebar entry; new `/[locale]/payouts/[id]` batch detail (derived status, four totals, recipients with failure reasons and per-row retry, activity timeline) with `loading`/`not-found`; `server/data/payouts.ts` (batches own recipients; `deriveStatus`/`summarise`, overview, schedule settings, bank accounts, CSV) and client-safe `lib/payout-status.ts` + `lib/payout-csv.ts` (shared parser/validator used by both the browser preview and the Server Action); Server Actions `createBatchAction`, `approveBatchAction`, `cancelBatchAction`, `retryBatchAction`, `retryRecipientAction`, `updatePayoutScheduleAction`, `setDestinationAccountAction`, `addBankAccountAction`; components `<PayoutsSummaryCards/>` (replacing the broken `,250,890.00` literals), `<BatchFilters/>`, `<BatchesTable/>`, `<BatchesEmptyState/>`, `<BatchUploadDropzone/>` (real file input, drag & drop, 2 MB guard), `<RecipientPreviewTable/>` (valid/rejected tabs, per-line reasons, rejected-rows export), `<CreateBatchDialog/>` (`?new=1`), `<ReleaseBatchDialog/>` (`?send=1`/`?cancel=1`, confirmation-gated), `<RecipientsTable/>`, `<BatchTimeline/>`, `<RetryFailuresButton/>`, `<PayoutScheduleForm/>` (dirty state, conditional weekday/day-of-month, parsed threshold), `<DestinationAccountDialog/>` (verification gate + add account), `<PayoutStatusPill/>`/`<RecipientStatusPill/>`; export routes `/api/exports/payouts`, `/api/exports/payouts/[id]`, `/api/exports/payout-template` (the old `href="#"` template link). Tests: `src/server/data/payouts.test.ts`, `src/lib/payout-csv.test.ts`, `src/components/payouts/payout-status-pill.test.tsx`, `e2e/payouts.spec.ts`; manual procedure in `docs/audit/payouts-test-procedure-2026-09-01.md`; ADR-0010.
- **2026-09-01** — Settings pass of the flow audit: new `/[locale]/settings` hub (the route previously 404'd) with per-section live status from `getSettingsOverview()`, `<SettingsNav/>` mounted on all five routes and a `/settings` sidebar entry; `server/data/settings.ts` (merchant profile, notification channels/topics, API keys, developer settings) + client-safe `lib/settings-options.ts` validators; Server Actions `updateMerchantProfileAction`, `updateNotificationChannelAction`, `updateNotificationPreferenceAction`, `createApiKeyAction`, `revokeApiKeyAction`, `rollApiKeyAction`, `addIpAllowAction`, `removeIpAllowAction`, `updateDeveloperToggleAction`; components `<MerchantProfileForm/>` (dirty-state sticky footer, inline errors, live colour swatch, unsaved-changes guard), `<NotificationPreferencesForm/>` (optimistic switches/selects, critical topics locked), `<ApiKeysTable/>`, `<CreateApiKeyDialog/>`, `<SecretReveal/>` (reveal-once), `<ApiKeyRowActions/>` (confirmed roll/revoke), `<IpAllowlistManager/>`, `<DeveloperToggle/>`; breadcrumbs and the docs card no longer point at `#` (`/settings`, `/support`, `/webhooks`); `loading.tsx` for all five routes. Tests: `src/server/data/settings.test.ts`, `src/lib/settings-options.test.ts`, `e2e/settings.spec.ts`; manual procedure in `docs/audit/settings-test-procedure-2026-09-01.md`; ADR-0009.
- **2026-09-01** — Billing pass of the flow audit: `server/data/invoices.ts` derives invoices from monthly ledger fees (prototype statements seeded, computed `DRAFT`/`PENDING`/`OVERDUE`/`PAID`, line items grouped by channel, timeline, statement CSVs); Server Actions `payInvoiceAction` / `payInvoicesAction` with an explicit confirmation gate and double-pay guard; `/[locale]/billing` rebuilt on URL state (`q`, `status`, `range`, `sort`, `page`) with real summary cards, an actionable overdue banner, clickable rows and pagination; new `/[locale]/billing/[id]` statement page (header, totals, line items, billed transactions, payment timeline, billing details) with `loading`/`not-found` at both levels; components `<BillingSummaryCards/>`, `<OverdueBanner/>`, `<InvoiceFilters/>`, `<InvoicesTable/>`, `<InvoiceStatusPill/>`, `<InvoiceRowActions/>`, `<PayInvoiceDialog/>` (`?pay=1`), `<DownloadInvoiceButton/>`, `<InvoicesEmptyState/>`, `<InvoiceHeader/>`, `<InvoiceTotals/>`, `<InvoiceLineItems/>`, `<InvoicePaymentTimeline/>`; export routes `/api/exports/invoices` and `/api/exports/invoices/[id]`; breadcrumb switched to the locale-aware `Link`. Tests: `src/server/data/invoices.test.ts`, `invoice-status-pill.test.tsx`, `e2e/billing.spec.ts`; manual procedure in `docs/audit/billing-test-procedure-2026-09-01.md`; ADR-0008.
- **2026-09-01** — Customers pass of the flow audit: real directory at `/[locale]/customers` (URL-driven `q`/`status`/`sort`/`page`, selection + bulk actions, formatted LTV) and the new profile route `/[locale]/customers/[id]`; `server/data/customers.ts` derives customers from the ledger (prototype rows seeded, not deleted) with `createCustomer`/`updateCustomer`; Server Actions `create`/`update`/`archive`; `<CreateCustomerDialog/>` (`?new=1`), `<EditCustomerDialog/>` (`?edit=1`), `<CustomerFilters/>` (`?q=`), `<CustomersTable/>`, `<CustomerRowActions/>`, `<CustomerHeader/>`, `<CustomerStatusMenu/>`, `<CustomerLifetimeStats/>`, `<CustomerPaymentMethods/>`, `<CustomerTransactionsPanel/>`, `<CustomerEmptyState/>`, `<CustomerAvatar/>`, loading/not-found for both routes, CSV route `/api/exports/customers`. Proxy finally executes via `src/middleware.ts` (Next ignores a root `middleware.ts` when `src/` exists): `/` and `/api/*` pass through, bare `/sign-in`/`/sign-up` and `/id` are rewritten, unmatched paths render the in-shell 404, auth redirect gated behind `AUTH_ENFORCED=1`. Tests: `src/server/data/customers.test.ts`, `customer-status-pill.test.tsx`, `e2e/customers.spec.ts`, `e2e/routing.spec.ts`; manual procedure in `docs/audit/customers-test-procedure-2026-09-01.md`; ADR-0007.
- **2026-09-01** — Flow completeness pass on `dashboard` → `transactions` → `transactions/[id]`: `<CreateTransactionDialog/>`, `<RefundDialog/>`, retry/row-action menus, URL-driven filters + pagination, clickable rows, CSV export route `/api/exports/transactions`, cookie-backed Setup Progress checklist, sonner toasts, `<Suspense>` skeletons, filter-aware empty states, `error.tsx`/`not-found.tsx`, sidebar active state — gap analysis in `docs/audit/user-flow-audit-2026-09-01.md`, ADR-0006.
- **2026-08-31** — Milestones 3-6: Observability Sentry+OTEL `instrumentation.ts`+`instrumentation-client.ts`+`sentry.*.config.ts` + `WebVitals` pino `track()`; i18n `next-intl` `routing.ts`+`request.ts` `messages/en, id` `withNextIntl`+`NextIntlClientProvider`; 3D `three@0.185`+`@react-three/fiber`+`@react-three/drei` `Hero3D` `dynamic(ssr:false)`; Testing Vitest+RTL+Playwright `vitest.config.ts`+`e2e/smoke.spec.ts`+`ci.yml` `pnpm test` 3 passed — ADR-0005/0002/0001.
- **2026-08-31** — Migrate `fraud` (`table+tabs+switch+badge` `fraud_prevention_desktop`), `fraud/blocklist` (`table+badge`), `kyc` (`card+progress+accordion+input` `identity_verification_kyc`) — shadcn `AGENTS.md:20` `switch/tabs/calendar`, 13 routes, codegraph 145 files wal — 2026-08-31 ADR-0002.
- **2026-08-31** — Migrate remaining 14 routes (17 rows) `settings/api-keys`, `reports/builder`, `settings/developer`, `payments/links`, `payouts/settings`, `subscriptions`, `team`, `webhooks`, `settings/merchant`, `settings/notifications`, `risk`, `onboarding`, `support`, `system` — shadcn 100+ (`table, avatar, badge, select, tabs, calendar` etc.) per `AGENTS.md:20`, 27 routes, codegraph 162 files wal — 2026-08-31 ADR-0002.
- **2026-08-31** — shadcn 100+ via `registry.directory` (62 `add -a` + 32 `@diceui/@tailark` → 94 `ui/*.tsx` + 6 `layout/*` kept, `primitive-elements.md` audit, `globals.css:25` `--primary:#003fb1` restored) — typecheck/build pass, codegraph 141 files wal.
- **2026-08-31** — Migrate `customers` (`table+avatar+badge+pagination+checkbox` `customer_directory:257`), `bulk_payouts` (`file-upload+progress`), `billing` (`select+tabs+calendar`), `audit` (`tabs+select+calendar+checkbox`) — shadcn primitives per `AGENTS.md:20`, 10 routes, ADR-0002.
- **2026-08-31** — Parse 33 screens → `docs/screens-index.json` + `tokens-diff.md`; merge tokens `warning` alias; layout primitives `sidebar/top-bar/bottom-nav/metric-card/data-table`; migrate `dashboard/balance/transactions` (responsive `lg:grid-cols-12`, `label-caps` sticky, `data-mono` right, DAL `ledger` #106, `xendit` #31) — typecheck/build pass, codegraph 25→33 files wal, ADR-0001/0002.
- **2026-08-30** — Phase 0 scaffold: `apps/web` Next.js 15 + TS + Tailwind 4 + Kinetic Ledger/Enterprise tokens as CSS vars + TEST MODE #d97706 banner + shadcn button/card + `app/[locale]/dashboard` (typecheck/build pass, ADR-0001/0002).
- **2026-08-30** — Docs scaffold: `docs/adr/` (5 ADRs), `docs/STACK.md`, `docs/ARCHITECTURE.md`, `PROGRESS.md` → phased tracker, `SCREENS.md` migrated-route column.
- **2026-08-29** — Reorganized repo into `design-system/` and `screens/{mobile,desktop}`; added `README.md`, `AGENTS.md`, `PROGRESS.md`, `SCREENS.md`.
