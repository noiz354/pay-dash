# Known Debt Register — PayDash (post Wave 0–4, finalized in Wave 5)

> **Rule:** only debts that *actually exist at `b953cc9`* are listed. Each row is verified against the code or an executed check, not copied from an old report. "Verification debt" rows are honest PENDING/NOT_RUN/BLOCKED items — they disappear the moment the evidence is produced (then update the register in the same PR).
> **Priority:** P0 = blocks release/READY or is a security exposure · P1 = fix in Wave 5 · P2 = fix next scheduled wave · P3 = tracked, fix opportunistically · WONTNOW = consciously deferred by spec MoSCoW.
> **Wave ownership (added 2026-09-13):** every open row is now chartered by a spec — the authoritative debt → wave map is `WAVE_ROADMAP_7D_TO_11.md` §3. Summary: D-01..D-04 + D-21 + D-22 → **Wave 9**; D-05..D-08 + D-20 → **Wave 8**; D-09 + D-14 + D-18 + D-19 → **Wave 10**; D-10..D-13 + D-15 + D-16 → **Wave 11**; D-26 + D-27 → **Wave 7H**; D-28 → **Wave 7E**; D-17 is the standing lint baseline every wave must not worsen; D-23/D-24/D-25 stay **WONTNOW** by MoSCoW and are deliberately unchartered (Wave 10 and Wave 11 both list them as out of scope). A row is closed only in the Q7 that produces its evidence — never pre-emptively.

| ID | Debt | Risk | Impact | Priority | Recommendation |
|---|---|---|---|---|---|
| D-01 | **Playwright Wave 4 gates never executed** (8 tests / 6 specs in `apps/web/e2e/wave4/`; authoring + Wave 5 sandboxes OOM at 4 GB RAM) | Unverified browser behavior on the safety-critical journeys (dual-control refund, 409, permission denial, SLA URL contract, mobile) | A defect in any of the six gates would ship undetected | **P0** (release gate) | Execute on ≥ 8 GB host or CI: `pnpm test:e2e -- e2e/wave4 --project=chromium` (procedure: `WAVE_4_E2E_TEST_PLAN.md`). Paste raw output into `WAVE_4_IMPLEMENTATION_REPORT.md` §8. On FAIL: triage before any READY claim. |
| D-02 | **Full E2E inventory NOT_RUN** (~224 tests / 30 specs) | Regression net for the whole redesign unexecuted in this session | Unknown latent regressions outside the six gates | P1 | Run `npx playwright test` in CI/normal host; triage failures to `docs/QA_ISSUES.md`. |
| D-03 | **Performance budgets unmeasured** (LCP < 2.5 s, INP < 200 ms, CLS ≤ 0.1 / target 0.05); in-sandbox production build BLOCKED_BY_ENVIRONMENT (font CDN + RAM) | Budgets may be missed without knowing; Hero 3D keep/replace decision blocked | User-facing latency risk on dashboard/ledger; spec §25 acceptance not evidenced | P1 | `next build` + `next start` on a normal host → web-vitals via CDP per `QA_HANDOFF_WAVE_5.md` §8 (P1–P6). Then decide Hero 3D per spec (replace only if LCP > 2.5 s). |
| D-04 | **Accessibility verification is "touched screens only"** — full-route axe-core sweep not run | Untouched routes may carry violations (e.g. base-app screens) | WCAG 2.2 AA claim is partial | P1 | Full-route sweep (all routes × en/id × light/dark) via axe-core in the e2e env; file violations to `docs/QA_ISSUES.md`. |
| D-05 | **Sign-in rate limiting not implemented** (spec §8: 5/min/IP + captcha after 3 fails → 429). Rate limiting exists only for the AI-journal API. | Brute-force / credential stuffing on `/sign-in` | P0-class auth hardening gap in production | P1 (security) | Add IP-keyed limiter in the proxy/middleware for the sign-in path + 429 with `Retry-After`; unit test the window. |
| D-06 | **MCP endpoint rate limiting missing** (TODO.md R1) | MCP surface (tools/call) unthrottled | Abuse / DoS vector on the API-surface | P0 (security, pre-existing) | `assertMcpRateLimit` module (IP + token key, 10-min window) called in `handleMcpRequest` before auth/tools; unit test. |
| D-07 | **MCP call audit logging missing** (TODO.md R2) — tool, args digest, ok/err, timestamp not recorded | Non-repudiation gap for the MCP surface; weakens the "everything is auditable" invariant | Compliance/forensics | P1 (pre-existing) | Log each `tools/call` to the audit seam (`server/data/audit.ts` / pino); unit test. |
| D-08 | **MCP token comparison not constant-time** (TODO.md R3, `crypto.timingSafeEqual` in `src/server/mcp/auth.ts`) | Timing side-channel on token comparison | Low-probability, high-embarrassment security finding | P1 (pre-existing) | Equal-length check + `timingSafeEqual`; unit test. |
| D-09 | **Persistence seam is in-memory stores** (dev/preview); Prisma is wired only on MCP/audit paths; server restart resets ledger state. *Wave 7A update:* the transaction seam is now tenant-partitioned (`Map<organizationId, rows>`) and the unscoped accessors fail closed once a second tenant exists; 10 legacy ledger surfaces remain quarantined (`server/data/transactions-unscoped.ts`, was 12 until Wave 7D removed `invoices` and Wave 7F removed `onboarding`) | Data loss on restart in TEST mode; dev/preview divergence from real deployment | Operational confusion; "where is my data?" questions | P2 | Keep the seam (it is deliberate for TEST mode); document per-environment behavior in `docs/ARCHITECTURE.md`; do not couple UI work to the store choice. Wave 7B deletes one `LEGACY_LEDGER_SURFACES` entry per module. |
| D-10 | **SLA policies hardcoded** (`SLA_POLICIES`, `lib/sla.ts`) — no admin UI | Business can't adjust commitments without a deploy | Slow response to SLA policy changes | P2 | One-line policy change + test today; build an admin surface only when policies churn (spec: Wave 5+ candidate). |
| D-11 | **Analytics dashboards absent** — events (ANA-001..014 + 9 W4 operational) are emitted but no funnel dashboard consumes them | The "measure every journey" promise is half-delivered; backlog prioritization can't use funnel drop-off | Product/PM blind spot | P1 (Wave 5 scope) | Build the private `analytics/` dashboard over the existing events (spec §26); no new vendor. |
| D-12 | **QuickPay drawer (SCR-038 / FE-005) never built** — single-payment creation works via the existing dialog, not the spec's 4-field drawer | Primary creation flow is less efficient than specified (11-click legacy path partially mitigated) | Productivity target (32 s create) not fully met | P2 | Implement per spec §13 (mask + focus trap + optimistic row) as a focused ticket; update traceability row JRN-002 PARTIAL → implemented. |
| D-13 | **BE-006 invite 7-day expiry cron not implemented** — invites never auto-expire; the SLA nudge (5 d approaching / 2 d critical on `team_invite`) exists but nothing enforces the 7-day cutoff | Stale invites accumulate; spec §32 contract half-met | Governance hygiene | P2 | Cron/queue that expires pending invites + `INVITE_EXPIRED` audit entry (spec §43 BE-006 DoD); unit test. |
| D-14 | **`next/font/google` network dependency** — `app/layout.tsx` fetches Geist/Inter/JetBrains Mono at build/dev; blocks offline builds (the in-sandbox build blocker) and slows cold compiles (retry storm on TLS-blocked nets) | Build fragility in restricted networks; slow sandbox iteration | DX + CI robustness | P2 | Self-host the fonts (static woff2 + local `next/font/local`) or vendor them into the repo; drop the runtime fetch. (Durable fix; the mock-responses env flag is NOT a substitute — it needs a mocked-responses JSON file.) |
| D-15 | **Offline queue (IndexedDB) for bulk/CSV retry not implemented** (spec Phase 4) | Disconnected operators can't queue work; failures are surfaced but not queued | Mobile/field resilience gap | P2 (spec Phase 4) | Implement per spec §24 (offline draft persists CSV to retry) after the core gates are green. |
| D-16 | **Legacy tables coexist** (`transactions-table.tsx`, `batches-table.tsx` retained; customers already migrated) | Two implementations to maintain; drift risk | Maintenance cost | P3 | Intentional coexistence (Wave 2 decision §55). Migrate remaining consumers screen-by-screen (customers = reference); delete legacy only after the last consumer is gone and e2e is green. |
| D-17 | **40 pre-existing lint warnings** (e.g. `react-hooks/exhaustive-deps`) | Signal decay — reviewers stop reading warnings | Review quality | P3 | Batch-fix in a dedicated PR (no mixing with feature work); make "zero new warnings" the standing review rule (already in `PR_REVIEW_GUIDE.md`). |
| D-18 | **Middleware indirection quirk** — root `middleware.ts` + `src/middleware.ts` + logic in `src/proxy.ts`; Next's `src/` resolution rule makes this fragile to future moves | A refactor could silently disable the auth gate (the original bug was exactly this: a root file that was never loaded) | P0-class fragility, currently documented + tested | P3 | Keep the documented re-export chain + `proxy.test.ts` as the guard; if Next ever changes `src/` resolution, the e2e `routing.spec.ts` (auth redirect assertions) must catch it. |
| D-19 | **Sentry config files deprecated** (`sentry.server.config.ts`, `sentry.edge.config.ts`, `sentry.client.config.ts` → instrumentation-file pattern) | Startup warnings; SDK may miss `onRequestError` hooks (per the deprecation text) | Error-capture gaps | P3 | Move SDK init into `instrumentation.ts` per the warning; delete the three config files; verify capture end-to-end once. |
| D-20 | **429 UX not finished** — "Too many requests — Retry after …" toast path exists in the track/events layer; the `Retry-After` header consumption in the UI is partial (Wave 2 deferral) | Degraded error experience under rate limiting | Minor UX | P3 | Finish the `Retry-After` consumption in the mutation surface (one handler + test) together with D-05/D-06. |
| D-21 | **MCP sweep follow-up incomplete** (TODO.md (a): live `get_webhook_event` sweep row #20) | Sweep doc stale vs capability | Docs accuracy | P3 | Run the sweep, append to `docs/MCP_SWEEP.md`. |
| D-22 | **`WAVE_1_IMPLEMENTATION_REPORT.md` missing from the repo** — Wave 1 evidence exists only in `IMPLEMENTATION_PROGRESS.md` (ticket rows + test counts) and the Wave 2 report's baseline section | Auditability gap: Wave 1 (grouped nav, resolver, permission adapter, chrome) has no standalone report | Auditability (Wave 5 charter) | P2 | Reconstruct from the progress registry + git history (route-resolver 20/20, permission-adapter 12/12, sidebar 9/9, bottom-nav 5/5, proxy.alias 6/6 — all green in the 1075 run) in a docs-only PR. |
| D-23 | **Firebase SSO bridge (AI Journal)** | Double-login friction for the journal surface | Minor UX | WONTNOW (spec Phase 4 / MoSCoW) | Implement only when the journal moves to GA. |
| D-24 | **Web push notifications** | Notification gap | Minor UX | WONTNOW (spec MoSCoW) | Revisit if the exception-first dashboard proves insufficient for reach. |
| D-25 | **Multi-currency** (IDR + USD enum exists in schemas; single-currency UX) | No | — | WONTNOW (spec MoSCoW) | Out of scope. |
| D-26 | **`LedgerEntry` has no `organizationId` column** (schema.prisma:75) — so the Postgres ledger cannot be tenant-scoped. Wave 7A therefore *refuses* it: `server/dal/ledger.ts` throws on every entry point, and `pg-stores`' unscoped transaction readers were deleted (MCP `dataSource=postgres` → refusal for transaction tools) | If the refusal were removed before the column lands, every Postgres transaction read is cross-tenant; `getBalanceOverviewPostgres` still aggregates the table unscoped (Wave 7B) | P0 (security, blocks multi-tenant Postgres deployment) | **P0** | Migration: add `organizationId` + `@@unique([id, organizationId])` + `@@index([organizationId, createdAt])`, backfill from `OrganizationMember`/`userId`, then re-open `server/dal/ledger.ts` with `where: { organizationId }` and enable **RLS** (`current_setting('app.org_id')`). Bind MCP tokens to an organization while here (removes the shared-bearer-tenant-less pattern Wave 7A had to work around). Grep-guard: `S-6` in `transactions-structural.test.ts`. |
| D-28 | **Remaining unscoped payout readers (Wave 7C update: customers slice done)** — `balance.ts:198`, `audit.ts:113`, `command-center.ts:141`, `handoff.ts:142`, `finance/snapshot.ts:121`, `reports/builder/page.tsx:23` still read via `payouts-unscoped.ts` (6 readers). Customers quarantine is **1** surface (`reports`) via `customers-unscoped.ts` — also fail-closed; **Wave 7D removed the `subscriptions` entry** when the subscriptions page moved to the scoped DAL. Total unscoped modules now **11** (was 15 — `team`, `settings`, `kyc` and `onboarding` done in Wave 7F). **Wave 7F cleared the `onboarding` entry in `LEGACY_LEDGER_SURFACES`** (11 → 10) when the checklist moved to the scoped `getLedgerRows(ctx)`, and dropped `server/data/onboarding.ts` from S-2's `ALLOWED_UNSCOPED_CONSUMERS` (10 → 9) — the clearance this row's ES-6 gate needs. It also *added* three slice quarantines (**D-29**), which are explicitly **not** ES-6 targets: ES-6 is pinned to the three legacy paths by name. | In a multi-tenant deployment these derived surfaces throw (fail-closed) instead of serving — safe but unavailable | Six derived surfaces (balance, audit, command-center, handoff, finance snapshot, reports builder) stay dark the moment a second tenant exists, and the two allowlists cannot reach zero — which blocks Wave 7E's ES-6 deletion gate and keeps all three legacy quarantine modules in the tree | P1 — **OWNED: Wave 7E** | **Chartered in `WAVE_7E_DERIVED_SPEC.md` (Proposed) — this row is D-28's owner of record; see that spec's P-11.** The six line numbers above are the *quarantine consumption sites* (`legacyPayoutBatches(...)`, re-verified accurate at `af18cc4`); the spec's P-1..P-6 cite the *unscoped aggregate definitions* to scope (`balance.ts:272,313,362,397,436` · `audit.ts:131,231,269` · `command-center.ts:138` · `handoff.ts:136,348,372,386` · `finance/snapshot.ts:201`), and P-9 covers the `reports/builder` page wiring. Recipe: scope each to its session tenant like Q4 did for exports/MCP (customers: `resolveCustomerOrganizationContext`), shrink allowlists to zero, delete the three legacy quarantines — deletion gate **ES-6** (pinned to the three legacy paths by name; slice quarantines from 7D/7F/7G are out of scope and tracked in `WAVE_ROADMAP_7D_TO_11.md` §4). Grep-guards: Q-2 in `payouts-structural.test.ts` + `customers-structural.test.ts`. Closes at 7E Q7 with **ADR-0045** + report + matrix. |
| D-29 | **Wave 7F identity slice quarantines ride Wave 7E/7G derived surfaces** — `server/data/team-unscoped.ts` (`LEGACY_TEAM_SURFACES = ["audit"]`), `server/data/settings-unscoped.ts` (`LEGACY_SETTINGS_SURFACES = ["audit"]`) and `server/data/kyc-unscoped.ts` (`LEGACY_KYC_SURFACES = ["handoff"]`, the spec's conditional module) exist because their only consumer is a derived surface owned by another wave. Separately, `onboarding.ts`'s webhook count degrades to `0` when 7G's process-wide `listWebhooks` refuses (its store seeds from the 7A ledger quarantine) — matched by error **name**, not import, so `onboarding.ts` does not become a quarantine consumer (FS-4 would rightly fail). | In a multi-tenant deployment `audit.ts` (audit log) and `handoff.ts` (handoff queue, incl. its `kyc_review` lane) throw instead of serving; onboarding's Technical Setup item under-reports callback events as 0 | Loud, not leaky — three derived surfaces stay dark above one tenant, and the three allowlists cannot reach zero on their own | P1 — **OWNED: Wave 7E** (audit + handoff) / **Wave 7G** (webhooks) | Scope `audit.ts` and `handoff.ts` to their session tenant in 7E (they read the ledger, payouts, webhooks *and* identity), shrink all three allowlists to zero and delete the modules per 7E's P-10 criterion (**deletion is earned, not scheduled**); 7G's scoped webhook store retires the `onboarding` degradation. Survivors recorded in `WAVE_ROADMAP_7D_TO_11.md` §4.2 with the caller that forced each. Grep-guards: FS-4 in `identity-structural.test.ts` (existence, `@deprecated`, read-only, frozen allowlists, prod-path scan, consumer-set pin). Closes at 7E Q7 with **ADR-0045**. |
| D-27 | **Analytics events carry no tenant dimension**, and the ledger surface emits via `lib/analytics.ts#track()` instead of the allowlisted `trackEvent()` | In a multi-tenant deployment, funnel events from different organizations are indistinguishable in one stream, and unenforced props could reach the wire (no row content or resource id is sent today, so this is metadata granularity, not a data leak) | Funnel/segmentation blind spot; weakens per-tenant SLO attribution | P2 | Route the transaction components through `trackEvent` with catalog entries, add an `org` dimension (hashed org id, never the raw id), and assert the allowlist in `analytics-events.test.ts`. Bundle with Wave 7B's audit/analytics pass. |

## Verification-debt closure checklist (the rows that should vanish first)

- [ ] D-01 → raw Playwright gate output in `WAVE_4_IMPLEMENTATION_REPORT.md` §8 → status PASS or FAIL (honest)
- [ ] D-02 → full E2E totals + triage
- [ ] D-03 → performance numbers in `UX_REDESIGN_FINAL_IMPLEMENTATION_REPORT.md` §16 + Hero 3D decision
- [ ] D-04 → full-route axe sweep results
- [ ] D-22 → `WAVE_1_IMPLEMENTATION_REPORT.md` reconstructed

When a row closes: delete it here and cite the PR/commit in the final report's §17/§22.

*Register maintained per Wave 5 charter rule 9 (all known debt visible) and rule 6 (no duplicate prose — each row is the single home for its debt).*
